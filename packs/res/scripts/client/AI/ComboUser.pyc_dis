# Embedded file name: ./scripts/client/AI/ComboUser.py
from Config import Monsters as Config

class ComboUser:

    def on_combo_started(self, combo_index):
        combo = Config.get_combos(self.type_id)[combo_index]
        actions = self.model
        for action in combo['actions']:
            actions = getattr(actions, action)()