# Embedded file name: ./client/journal/rep_tab/show_hide_group.py


class ShowHideGroup(object):
    """ \xd0\xba\xd0\xbb\xd0\xb0\xd1\x81\xd1\x81 \xd0\xbf\xd1\x80\xd0\xb8\xd0\xbd\xd0\xb8\xd0\xbc\xd0\xb0\xd0\xb5\xd1\x82 \xd1\x81\xd0\xbe\xd0\xb1\xd1\x8b\xd1\x82\xd0\xb8\xd1\x8f on_show, on_hide, set_group \xd0\xb8 \xd0\xbf\xd1\x80\xd0\xb5\xd0\xbe\xd0\xb1\xd1\x80\xd0\xb0\xd0\xb7\xd1\x83\xd0\xb5\xd1\x82 \xd0\xb8\xd1\x85 \xd0\xb2 \xd0\xbe\xd0\xb4\xd0\xbd\xd0\xbe \xd1\x81\xd0\xbe\xd0\xb1\xd1\x8b\xd1\x82\xd0\xb8\xd0\xb5 on_group_changed """

    def __init__(self):
        self._shown = False
        self._item_data = None
        return

    def on_show(self):
        if self._shown:
            return
        else:
            self._shown = True
            if self._item_data:
                self.on_group_changed(self._item_data, None)
            return

    def on_hide(self):
        if not self._shown:
            return
        else:
            self._shown = False
            if self._item_data:
                self.on_group_changed(None, self._item_data)
            return

    def set_group(self, item_data):
        if item_data and not item_data.has_value:
            item_data = None
        if self._item_data == item_data:
            return
        else:
            prev_item_data = self._item_data
            self._item_data = item_data
            if self._shown:
                self.on_group_changed(self._item_data, prev_item_data)
            return

    def on_group_changed(self, new_item_data, old_item_data):
        pass

    @property
    def item_data(self):
        if not self._shown:
            return None
        else:
            return self._item_data