# Embedded file name: ./client/journal/journal_win.py
import BWPersonality
import GUI
from Helpers.PyGUI.PyGUIBase import DragWin
from Localization import lc
from soGUI.soButton import soButton
from journal_tab_view import JournalTabView
from soGUI.GUIPositions import GUIPositions

class JournalWin(DragWin, GUIPositions):

    def __init__(self):
        comp = GUI.Window()
        comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
        comp.horizontalAnchor, comp.verticalAnchor = ('LEFT', 'TOP')
        comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
        comp.width, comp.height = (860, 620)
        comp.position = (100, 100, 0.5)
        comp.visible = False
        DragWin.__init__(self, comp)
        GUIPositions.__init__(self)
        self.component.script = self
        comp = GUI.Frame2('soGUI/frame/basic_frame_1.dds')
        comp.horizontalPositionMode, comp.verticalPositionMode = ('CLIP', 'CLIP')
        comp.widthMode, comp.heightMode = ('CLIP', 'CLIP')
        comp.horizontalAnchor, comp.verticalAnchor = ('CENTER', 'CENTER')
        comp.width, comp.height = (2, 2)
        comp.position = (0, 0, 1)
        comp.materialFX = 'BLEND'
        self.component.addChild(comp, 'frame')
        comp = GUI.Text()
        comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
        comp.horizontalAnchor, comp.verticalAnchor = ('LEFT', 'TOP')
        comp.font = 'RobotoCondensedRegular_16.font'
        comp.position = (14, 6, 0.3)
        self.component.addChild(comp, 'title')
        comp = GUI.Window()
        btn = soButton(comp)
        comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
        comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
        comp.horizontalAnchor, comp.verticalAnchor = ('RIGHT', 'TOP')
        comp.width, comp.height = (18, 18)
        comp.position = (self.component.width - 6, 6, 0.2)
        self.component.addChild(comp, 'closeBox')
        btn.onBound()
        btn.initVSC('soGUI/elements/closeBox.xml')
        btn.onClick = self.hide
        comp = GUI.Window()
        comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
        comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
        comp.horizontalAnchor, comp.verticalAnchor = ('LEFT', 'BOTTOM')
        comp.position = (0, self.component.height, 0.5)
        self.component.addChild(comp, 'tabview')
        comp.width, comp.height = self.component.width, self.component.height - 29
        self._tabview = JournalTabView(comp)
        self.onBound()

    def on_change_owner(self):
        self._tabview.on_change_owner()

    def show(self, tab_index = None):
        if self.component.visible:
            return
        self.component.title.text = lc('GUI.QuestLog.WINDOW_CAPTION') + BWPersonality.get_action_key('QuestJournal')
        GUIPositions.show(self)
        self.component.visible = True
        self._tabview.on_show(tab_index=tab_index)
        BWPersonality.GUICore.setBestCursor()

    def hide(self):
        if not self.component.visible:
            return
        self.component.visible = False
        self._tabview.on_hide()
        BWPersonality.GUICore.setBestCursor()

    def show_reptab(self, group_id):
        if not self.component.visible:
            self.show()
        self._tabview.show_reptab(group_id)

    def stop_background_tasks(self):
        self._tabview.stop_background_tasks()

    def on_knowledge_npc_event2(self, event_type, npc_id):
        self._tabview.on_knowledge_npc_event3(event_type, npc_id)

    def dungeons_data_updated3(self, quests_data):
        self._tabview.dungeons_data_updated4(quests_data)

    def got_events_data4(self, quests_data):
        self._tabview.got_events_data5(quests_data)

    def get_selected_quest_id(self):
        return self._tabview.get_selected_quest_id()

    def on_got_new_stories(self, stor_ids):
        self._tabview.on_got_new_stories(stor_ids)

    def mark_quest_status(self, quest_id, value):
        self._tabview.mark_quest_status(quest_id, value)

    def on_stories_wiped(self):
        self._tabview.on_stories_wiped()

    def on_got_hunt0(self, data):
        self._tabview.on_got_hunt1(data)

    def on_hunt_notification_got1(self, space_name, position):
        self._tabview.on_hunt_notification_got2(space_name, position)

    @property
    def visible(self):
        return self.component.visible

    def reset_position(self):
        """ \xd1\x81\xd0\xb1\xd1\x80\xd0\xb0\xd1\x81\xd1\x8b\xd0\xb2\xd0\xb0\xd0\xb5\xd1\x82 \xd1\x81\xd0\xbe\xd1\x85\xd1\x80\xd0\xb0\xd0\xbd\xd1\x91\xd0\xbd\xd0\xbd\xd1\x8b\xd0\xb5 \xd0\xbf\xd0\xbe\xd0\xbb\xd0\xbe\xd0\xb6\xd0\xb5\xd0\xbd\xd0\xb8\xd1\x8f \xd0\xbe\xd0\xba\xd0\xbe\xd0\xbd """
        self.component.position = (100, 100, 0.5)

    def stopDragging(self):
        DragWin.stopDragging(self)
        GUIPositions.stopDragging(self)