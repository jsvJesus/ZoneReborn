# Embedded file name: ./client/journal/sort_filter.py
import GUI
from soGUI.soButton import soButton, soButtonWTT
from Quest import SortMode as SM
from Localization import lc

class EventType:
    OnModeChanged, OnVisibleChanged = xrange(2)


class SortFilter(object):

    def __init__(self, component, listener, time_sort, select_all, filter_toggle):
        self.component = component
        self._time_sort = time_sort
        self._filter_toggle = filter_toggle
        self._listener = listener
        self._sort_mode = SM.TitleDown
        comp = GUI.Window()
        comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
        comp.width, comp.height = (559, 518)
        comp.materialFX = 'SOLID'
        comp.horizontalAnchor, comp.verticalAnchor = ('RIGHT', 'TOP')
        comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
        comp.position = (self.component.width - 7, 7, 0.4)
        comp.visible = False
        self.component.addChild(comp, 'filter_content')
        pos_x = 16
        comp = GUI.Window()
        btn = soButtonWTT(comp)
        btn.buttonStyle = soButton.TOGGLEBUTTON_STYLE
        btn.initVSC('soGUI/visual_styles/btn_toggle.xml')
        comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
        comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
        comp.horizontalAnchor, comp.verticalAnchor = ('LEFT', 'BOTTOM')
        comp.textureName = 'soGUI/elements/button_Z-A.dds'
        comp.materialFX = 'BLEND'
        comp.width, comp.height = (28, 28)
        comp.position = (pos_x, self.component.height - 8, 0.2)
        comp.visible = False
        self.component.addChild(comp, 'sortTitleBtnUp')
        btn.onBound()
        btn.onClick = self._toggle_titlesort
        btn.setVisualState('normal')
        self._titlesort_btn_up = btn
        comp = GUI.Window()
        btn = soButtonWTT(comp)
        btn.buttonStyle = soButton.TOGGLEBUTTON_STYLE
        btn.initVSC('soGUI/visual_styles/btn_toggle.xml')
        comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
        comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
        comp.horizontalAnchor, comp.verticalAnchor = ('LEFT', 'BOTTOM')
        comp.textureName = 'soGUI/elements/button_A-Z.dds'
        comp.materialFX = 'BLEND'
        comp.width, comp.height = (28, 28)
        comp.position = (pos_x, self.component.height - 8, 0.2)
        comp.visible = False
        self.component.addChild(comp, 'sortTitleBtnDown')
        btn.onBound()
        btn.onClick = self._toggle_titlesort
        btn.setVisualState('normal')
        self._titlesort_btn_down = btn
        if self._time_sort:
            pos_x += 30
            comp = GUI.Window()
            btn = soButtonWTT(comp)
            btn.buttonStyle = soButton.TOGGLEBUTTON_STYLE
            btn.initVSC('soGUI/visual_styles/btn_toggle.xml')
            comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
            comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
            comp.horizontalAnchor, comp.verticalAnchor = ('LEFT', 'BOTTOM')
            comp.textureName = 'soGUI/elements/button_up.dds'
            comp.materialFX = 'BLEND'
            comp.width, comp.height = (28, 28)
            comp.position = (pos_x, self.component.height - 8, 0.2)
            comp.visible = False
            self.component.addChild(comp, 'sortTimeBtnUp')
            btn.onBound()
            btn.onClick = self._toggle_timesort
            btn.setVisualState('normal')
            self._timesort_btn_up = btn
            comp = GUI.Window()
            btn = soButtonWTT(comp)
            btn.buttonStyle = soButton.TOGGLEBUTTON_STYLE
            btn.initVSC('soGUI/visual_styles/btn_toggle.xml')
            comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
            comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
            comp.horizontalAnchor, comp.verticalAnchor = ('LEFT', 'BOTTOM')
            comp.textureName = 'soGUI/elements/button_down.dds'
            comp.materialFX = 'BLEND'
            comp.width, comp.height = (28, 28)
            comp.position = (pos_x, self.component.height - 8, 0.2)
            self.component.addChild(comp, 'sortTimeBtnDown')
            btn.onBound()
            btn.onClick = self._toggle_timesort
            btn.setVisualState('normal')
            self._timesort_btn_down = btn
        if self._filter_toggle:
            pos_x += 30
            comp = GUI.Window()
            btn = soButtonWTT(comp)
            btn.buttonStyle = soButton.TOGGLEBUTTON_STYLE
            btn.initVSC('soGUI/visual_styles/btn_toggle.xml')
            comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
            comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
            comp.horizontalAnchor, comp.verticalAnchor = ('LEFT', 'BOTTOM')
            comp.textureName = 'soGUI/elements/button_filter.dds'
            comp.materialFX = 'BLEND'
            comp.width, comp.height = (28, 28)
            comp.position = (pos_x, self.component.height - 8, 0.2)
            self.component.addChild(comp, 'filterBtn')
            btn.onBound()
            btn.onClick = self._on_filter_click
            btn.setVisualState('normal')
            self._filter_btn = btn
        if select_all:
            self.component.filter_content.isClip = False
            comp = GUI.Window()
            btn = soButton(comp)
            comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
            comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
            comp.horizontalAnchor, comp.verticalAnchor = ('RIGHT', 'TOP')
            comp.label.font = 'RobotoCondensedRegular_16.font'
            comp.label.text = lc('GUI.QuestLog.SELECT_ALL')
            comp.materialFX = 'BLEND'
            comp.width, comp.height = (110, 28)
            comp.position = (self.component.filter_content.width - 98, self.component.filter_content.height + 10, 0.1)
            btn.initVSC('soGUI/elements/button_1.xml')
            btn.onBound()
            btn.onClick = self._on_select_all_click
            self._select_all_btn = btn
            self.component.filter_content.addChild(comp)
            comp = GUI.Window()
            btn = soButton(comp)
            comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
            comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
            comp.horizontalAnchor, comp.verticalAnchor = ('RIGHT', 'TOP')
            comp.label.font = 'RobotoCondensedRegular_16.font'
            comp.label.text = lc('GUI.QuestLog.DESELECT_ALL')
            comp.materialFX = 'BLEND'
            comp.width, comp.height = (96, 28)
            comp.position = (self.component.filter_content.width + 2, self.component.filter_content.height + 10, 0.1)
            btn.initVSC('soGUI/elements/button_1.xml')
            btn.onBound()
            btn.onClick = self._on_deselect_all_click
            self._deselect_all_btn = btn
            self.component.filter_content.addChild(comp)
        self.create_content()
        self._update_sort_visibility()
        self._update_filter_visiblity()

    def switch_off(self):
        self._filter_btn.setToggleState(False)
        self._update_filter_visiblity()

    def notify_mode_changed(self):
        self._listener.on_sortfilter_event(EventType.OnModeChanged)

    def create_content(self):
        pass

    def _on_select_all_click(self):
        pass

    def _on_deselect_all_click(self):
        pass

    def _on_filter_click(self):
        self._update_filter_visiblity()

    def _update_filter_visiblity(self):
        if self._filter_toggle:
            need_visible = self._filter_btn.buttonActive
            if self.visible != need_visible:
                self.component.filter_content.visible = self._filter_btn.buttonActive
                self._listener.on_sortfilter_event(EventType.OnVisibleChanged)

    def _update_sort_visibility(self):
        if self._sort_mode == SM.TitleDown:
            if self._time_sort:
                self._timesort_btn_down.setToggleState(False)
                self._timesort_btn_up.setToggleState(False)
            self._titlesort_btn_up.component.visible = False
            self._titlesort_btn_down.component.visible = True
            self._titlesort_btn_down.setToggleState(True)
        elif self._sort_mode == SM.TitleUp:
            if self._time_sort:
                self._timesort_btn_down.setToggleState(False)
                self._timesort_btn_up.setToggleState(False)
            self._titlesort_btn_up.component.visible = True
            self._titlesort_btn_down.component.visible = False
            self._titlesort_btn_up.setToggleState(True)
        elif self._sort_mode == SM.TimeDown:
            self._titlesort_btn_down.setToggleState(False)
            self._titlesort_btn_up.setToggleState(False)
            self._timesort_btn_down.component.visible = True
            self._timesort_btn_up.component.visible = False
            self._timesort_btn_down.setToggleState(True)
        elif self._sort_mode == SM.TimeUp:
            self._titlesort_btn_down.setToggleState(False)
            self._titlesort_btn_up.setToggleState(False)
            self._timesort_btn_down.component.visible = False
            self._timesort_btn_up.component.visible = True
            self._timesort_btn_up.setToggleState(True)
        else:
            raise RuntimeError

    @property
    def visible(self):
        return self.component.filter_content.visible

    def _toggle_titlesort(self):
        if self._sort_mode == SM.TitleDown:
            self._sort_mode = SM.TitleUp
        elif self._sort_mode == SM.TitleUp:
            self._sort_mode = SM.TitleDown
        elif self._titlesort_btn_up.component.visible:
            self._sort_mode = SM.TitleUp
        elif self._titlesort_btn_down.component.visible:
            self._sort_mode = SM.TitleDown
        else:
            raise RuntimeError
        self._update_sort_visibility()
        self.notify_mode_changed()

    def _toggle_timesort(self):
        if self._sort_mode == SM.TimeDown:
            self._sort_mode = SM.TimeUp
        elif self._sort_mode == SM.TimeUp:
            self._sort_mode = SM.TimeDown
        elif self._timesort_btn_up.component.visible:
            self._sort_mode = SM.TimeUp
        elif self._timesort_btn_down.component.visible:
            self._sort_mode = SM.TimeDown
        else:
            raise RuntimeError
        self._update_sort_visibility()
        self.notify_mode_changed()