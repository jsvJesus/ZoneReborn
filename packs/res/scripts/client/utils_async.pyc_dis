# Embedded file name: ./client/utils_async.py
import ResourceLoader

def load_model_from_list(model_list, on_load_callback):
    """
    \xd0\xb0\xd1\x81\xd1\x81\xd0\xb8\xd0\xbd\xd1\x85\xd1\x80\xd0\xbe\xd0\xbd\xd0\xbd\xd0\xb0\xd1\x8f \xd0\xb7\xd0\xb0\xd0\xb3\xd1\x80\xd1\x83\xd0\xb7\xd0\xba\xd0\xb0 \xd0\xbe\xd0\xb4\xd0\xb8\xd0\xbd\xd0\xbe\xd1\x87\xd0\xbd\xd0\xbe\xd0\xb9 \xd0\xbc\xd0\xbe\xd0\xb4\xd0\xb5\xd0\xbb\xd0\xb8 \xd0\xb8\xd0\xbb\xd0\xb8 \xd1\x81\xd0\xbe\xd1\x81\xd1\x82\xd0\xb0\xd0\xb2\xd0\xbd\xd0\xbe\xd0\xb9
    """
    if not model_list:
        print 'ERROR::failed load_model_from_list', model_list
        return
    if len(model_list) == 1:
        resource_list = ((ResourceLoader.MODEL, model_list[0]),)
    else:
        resource_list = ((ResourceLoader.MULTI_MODEL, model_list),)
    ResourceLoader.asyncLoad(resource_list, on_load_callback)


def load_model(model_path, on_load_callback):
    resource_list = ((ResourceLoader.MODEL, model_path),)
    ResourceLoader.asyncLoad(resource_list, on_load_callback)