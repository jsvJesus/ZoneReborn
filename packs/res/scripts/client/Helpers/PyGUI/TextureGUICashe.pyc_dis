# Embedded file name: ./client/Helpers/PyGUI/TextureGUICashe.py
"""
"""
import BigWorld
_cache = {}

def getGUITexture(texture_name):
    if not texture_name:
        return None
    else:
        if texture_name not in _cache:
            _cache[texture_name] = BigWorld.PyTextureProvider(texture_name)
        return _cache[texture_name]