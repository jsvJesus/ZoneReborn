# Embedded file name: ./client/clanboard_gui/base_tab_view.py
from Localization import lc
from tab_view import TabView
from table.clans_tab import ClansTab
from map.maps_tab import MapsTab
from recruit.recruit_tab import RecruitTab

class BaseTabView(TabView):

    def __init__(self, component):
        TabView.__init__(self, component)

    def create_tabs(self, components_provider):
        tabs = [{'caption': lc('clan_win.Clans.Title'),
          'tab': ClansTab(components_provider())}, {'caption': lc('clan_win.Recruit.Title'),
          'tab': RecruitTab(components_provider())}, {'caption': lc('clan_win.Captures.Title'),
          'tab': MapsTab(components_provider())}]
        return tabs