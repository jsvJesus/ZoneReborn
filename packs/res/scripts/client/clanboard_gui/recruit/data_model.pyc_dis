# Embedded file name: ./client/clanboard_gui/recruit/data_model.py
from utils import Listenable

class EventType:
    OnPageChanged, = ((r, 'RecruitDataModel') for r in range(1))


class DataModel(Listenable):

    def __init__(self):
        Listenable.__init__(self)
        self.page = []

    def set_page(self, page):
        if page == self.page:
            return
        self.page = page
        self.notify_listeners(EventType.OnPageChanged)