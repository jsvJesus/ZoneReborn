# Embedded file name: ./client/Damager.py
from generic.victimdamager import GenericDamager
import BigWorld

class Damager(GenericDamager):

    def on_victim_damaged(self, victim_id, damagetaken_data):
        return BigWorld.entity(victim_id, True)

    def on_victim_death(self, kill_data):
        return BigWorld.entity(kill_data['victim'], True)

    def get_height_idle_scaled(self):
        return 1.0