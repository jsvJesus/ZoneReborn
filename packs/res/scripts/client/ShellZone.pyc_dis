# Embedded file name: ./client/ShellZone.py
import BigWorld
from ClientFiguredTrapInterface import ClientFiguredTrapInterface
from client_utils import readable_space_pos

class ShellZone(BigWorld.Entity, ClientFiguredTrapInterface):
    """"
    \xd0\x97\xd0\xbe\xd0\xbd\xd0\xb0 \xd0\xb8\xd0\xbc\xd0\xb8\xd1\x82\xd0\xb0\xd1\x86\xd0\xb8\xd0\xb8 \xd1\x88\xd0\xb5\xd0\xbb\xd0\xbb\xd0\xb0
    """

    def __init__(self):
        BigWorld.Entity.__init__(self)
        ClientFiguredTrapInterface.__init__(self)

    def __repr__(self):
        if self.isDestroyed:
            return 'ShellZone[%s]Destroyed' % self.id
        return 'ShellZone[%s] at %s' % (self.id, readable_space_pos(self))

    def onPlayerInside(self):
        player = BigWorld.player()
        if not player or player.isDestroyed:
            return
        player.onEnterShellZone(self)

    def onPlayerOutside(self):
        player = BigWorld.player()
        if not player or player.isDestroyed:
            return
        player.onLeaveShellZone()

    def isOtsidePos(self, pos):
        """
        True, \xd0\xb5\xd1\x81\xd1\x82\xd1\x8c \xd1\x82\xd0\xbe\xd1\x87\xd0\xba\xd0\xb0 \xd0\xbd\xd0\xb5 \xd0\xb2 \xd0\xb7\xd0\xbe\xd0\xbd\xd0\xb5
        """
        return not self.is_pos_inside(pos)

    def onLeaveWorld(self):
        ClientFiguredTrapInterface.onLeaveWorld(self)