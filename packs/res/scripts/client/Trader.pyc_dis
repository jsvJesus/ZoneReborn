# Embedded file name: ./client/Trader.py
import Helpers.Caps
import barter_parser as bp

class Trader:

    def __init__(self):
        if Helpers.Caps.CAP_CAN_TRADE not in self.targetCaps:
            self.targetCaps += [Helpers.Caps.CAP_CAN_TRADE]
        self.BarterAssortment = bp.get_barter_assortment(self.getAdditionalParam('BarterListName'))