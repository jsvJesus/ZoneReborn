# Embedded file name: ./client/soGUI/StorehouseGUI/MessageWin.py
import GUI
from Helpers.PyGUI.PyGUIBase import PyGUIBase
from soGUI.UtilsGUI.PaintButton import PaintButton
from soGUI.StorehouseGUI.ShopUtils import MultilineText

class MessageWin(PyGUIBase):
    shop_instance = None
    paint_instance = None
    overlay_instance = None
    equippaint_instance = None
    _message_id = 0
    SHOP, PAINT, OVERLAY, EQUIPPAINT = xrange(4)

    def __init__(self, component, assign_instance):
        component.script = self
        component.mouseButtonFocus = True
        component.crossFocus = True
        PyGUIBase.__init__(self, component)
        if assign_instance == MessageWin.SHOP:
            MessageWin.shop_instance = self
        elif assign_instance == MessageWin.PAINT:
            MessageWin.paint_instance = self
        elif assign_instance == MessageWin.OVERLAY:
            MessageWin.overlay_instance = self
        elif assign_instance == MessageWin.EQUIPPAINT:
            MessageWin.equippaint_instance = self
        self._repositioners = []
        self._right_click_callback = None
        self._left_click_callback = None
        self._center_click_callback = None
        self._orange_click_callback = None
        self._setup_content()
        self.hide()
        return

    def _setup_content(self):
        comp = GUI.Window('textures/default/black.dds')
        comp.materialFX = 'SOLID'
        self.component.addChild(comp, 'content')
        button_height = 0.25
        button_width = 0.8
        comp = GUI.Window()
        comp.materialFX = 'SOLID'
        comp.widthMode, comp.heightMode = ('CLIP', 'CLIP')
        comp.width, comp.height = button_width, button_height
        comp.horizontalAnchor, comp.verticalAnchor = ('LEFT', 'BOTTOM')
        comp.horizontalPositionMode, comp.verticalPositionMode = ('CLIP', 'CLIP')
        comp.position = (-0.9, -0.9, 0.5)
        self._left_button = PaintButton(comp, visual_state='soGUI/frame/storehouse/storeButton.xml')
        self._left_button.on_click = self._on_left_click
        self._repositioners.append(self._left_button)
        self.component.content.addChild(comp, 'left_button')
        comp = GUI.Window()
        comp.materialFX = 'SOLID'
        comp.widthMode, comp.heightMode = ('CLIP', 'CLIP')
        comp.width, comp.height = button_width, button_height
        comp.horizontalAnchor, comp.verticalAnchor = ('RIGHT', 'BOTTOM')
        comp.horizontalPositionMode, comp.verticalPositionMode = ('CLIP', 'CLIP')
        comp.position = (0.9, -0.9, 0.5)
        self._right_button = PaintButton(comp, visual_state='soGUI/frame/storehouse/storeButton.xml')
        self._right_button.on_click = self._on_right_click
        self._repositioners.append(self._right_button)
        self.component.content.addChild(comp, 'right_button')
        comp = GUI.Window()
        comp.materialFX = 'SOLID'
        comp.widthMode, comp.heightMode = ('CLIP', 'CLIP')
        comp.width, comp.height = button_width, button_height
        comp.horizontalAnchor, comp.verticalAnchor = ('RIGHT', 'BOTTOM')
        comp.horizontalPositionMode, comp.verticalPositionMode = ('CLIP', 'CLIP')
        comp.position = (0.9, -0.9, 0.5)
        self._orange_button = PaintButton(comp, visual_state='soGUI/frame/storehouse/storeButtonOrange.xml')
        self._orange_button.on_click = self._on_orange_click
        self._repositioners.append(self._orange_button)
        self.component.content.addChild(comp, 'orange_button')
        comp = GUI.Window()
        comp.materialFX = 'SOLID'
        comp.widthMode, comp.heightMode = ('CLIP', 'CLIP')
        comp.width, comp.height = button_width, button_height
        comp.horizontalAnchor, comp.verticalAnchor = ('CENTER', 'BOTTOM')
        comp.horizontalPositionMode, comp.verticalPositionMode = ('CLIP', 'CLIP')
        comp.position = (0, -0.9, 0.5)
        self._center_button = PaintButton(comp, visual_state='soGUI/frame/storehouse/storeButton.xml')
        self._center_button.on_click = self._on_center_click
        self._repositioners.append(self._center_button)
        self.component.content.addChild(comp, 'center_button')
        comp = GUI.Window()
        comp.widthMode, comp.heightMode = ('CLIP', 'CLIP')
        comp.width, comp.height = (1.8, 1)
        comp.horizontalAnchor, comp.verticalAnchor = ('CENTER', 'TOP')
        comp.horizontalPositionMode, comp.verticalPositionMode = ('CLIP', 'CLIP')
        comp.position = (0, 0.4, 0.5)
        self._text = MultilineText(comp, u'', 'RobotoCondensedRegular_18')
        self._repositioners.append(self._text)
        self.component.content.addChild(comp, 'text')

    def _on_center_click(self):
        self.hide()
        if self._center_click_callback:
            self._center_click_callback()

    def _on_orange_click(self):
        self.hide()
        if self._orange_click_callback:
            self._orange_click_callback()

    def _on_right_click(self):
        self.hide()
        if self._right_click_callback:
            self._right_click_callback()

    def _on_left_click(self):
        self.hide()
        if self._left_click_callback:
            self._left_click_callback()

    def handleMouseButtonEvent(self, comp, event):
        return True

    def handleMouseClickEvent(self, comp):
        return True

    def show_double_buttons(self, text, left_button_text, right_button_text, left_click_callback = None, right_click_callback = None):
        if self.visible:
            self.hide()
        self._text.set_text(text)
        self._right_click_callback = right_click_callback
        self._left_click_callback = left_click_callback
        self._left_button.text = left_button_text
        self._right_button.text = right_button_text
        self._right_button.visible = True
        self._left_button.visible = True
        self._center_button.visible = False
        self._orange_button.visible = False
        return self._show()

    def show_double_orange_buttons(self, text, left_button_text, orange_button_text, left_click_callback = None, orange_click_callback = None):
        if self.visible:
            self.hide()
        self._text.set_text(text)
        self._orange_click_callback = orange_click_callback
        self._left_click_callback = left_click_callback
        self._left_button.text = left_button_text
        self._orange_button.text = orange_button_text
        self._right_button.visible = False
        self._left_button.visible = True
        self._center_button.visible = False
        self._orange_button.visible = True
        return self._show()

    def show_single_button(self, text, button_text, callback = None):
        if self.visible:
            self.hide()
        self._text.set_text(text)
        self._center_click_callback = callback
        self._center_button.text = button_text
        self._right_button.visible = False
        self._left_button.visible = False
        self._center_button.visible = True
        self._orange_button.visible = False
        return self._show()

    def _show(self):
        self.component.visible = True
        MessageWin._message_id += 1
        return MessageWin._message_id

    def hide(self):
        if not self.visible:
            return
        self.component.visible = False

    def doReposition(self):
        self.do_reposition()

    def do_reposition(self):
        ratio = 2.0
        comp = self.component.content
        comp.horizontalAnchor, comp.verticalAnchor = ('CENTER', 'CENTER')
        comp.horizontalPositionMode, comp.verticalPositionMode = ('CLIP', 'CLIP')
        comp.widthMode, comp.heightMode = ('PIXEL', 'CLIP')
        comp.height = 0.6
        comp.heightMode = 'PIXEL'
        comp.width = comp.height * ratio
        comp.position = (0, 0.1, 0.5)
        for r in self._repositioners:
            r.do_reposition()

    @property
    def visible(self):
        return self.component.visible

    @property
    def message_id(self):
        return self.component.visible and MessageWin._message_id or None