# Embedded file name: ./client/soGUI/cast_bar/castbar_bar.py
import GUI
from cast import Cast

class CastbarBar(Cast):

    def __init__(self):
        comp = GUI.Window('soGUI/frame/paint/white.dds')
        comp.materialFX = 'BLEND'
        comp.wm, comp.hm = ('PIXEL', 'PIXEL')
        comp.ha, comp.va = ('CENTER', 'CENTER')
        comp.hpm, comp.vpm = ('CLIP', 'CLIP')
        comp.width, comp.height = (200, 6)
        comp.position = (0, -0.5, 0.2)
        Cast.__init__(self, comp)
        comp = GUI.Window('soGUI/frame/paint/white.dds')
        comp.colour = (255, 255, 255, 170)
        comp.materialFX = 'BLEND'
        comp.wm, comp.hm = ('CLIP', 'CLIP')
        comp.ha, comp.va = ('LEFT', 'CENTER')
        comp.hpm, comp.vpm = ('CLIP', 'CLIP')
        comp.width, comp.height = (2, 2)
        comp.position = (-1, 0, 0.5)
        self.component.addChild(comp, 'filler')

    def _update_content(self):
        if self.ratio is None:
            self.component.visible = False
            return
        else:
            self.component.visible = True
            self.component.filler.width = self.ratio * 2
            return

    def _set_red_black(self, factor):
        self.component.colour = (factor * 100,
         0,
         0,
         153)