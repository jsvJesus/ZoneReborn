# Embedded file name: ./client/soGUI/scholar/scholar_bar.py
import GUI
import colorCodes as cc

class ScholarBar:

    def __init__(self, component):
        component.materialFX = 'SOLID'
        component.colour = (170, 170, 170, 255)
        self.component = component
        self._max_value = 0
        self._value = (0, 0)
        comp = GUI.Window('soGUI/frame/paint/white.dds')
        comp.materialFX = 'SOLID'
        comp.colour = (37, 37, 37, 255)
        comp.wm, comp.hm = ('PIXEL', 'PIXEL')
        comp.ha, comp.va = ('LEFT', 'TOP')
        comp.hpm, comp.vpm = ('PIXEL', 'PIXEL')
        comp.width, comp.height = component.width - 2, component.height - 2
        comp.position = (1, 1, 0.9)
        component.addChild(comp, 'back')
        comp = GUI.Window('soGUI/frame/paint/white.dds')
        comp.materialFX = 'SOLID'
        comp.colour = (77, 77, 77, 255)
        comp.wm, comp.hm = ('CLIP', 'CLIP')
        comp.ha, comp.va = ('LEFT', 'CENTER')
        comp.hpm, comp.vpm = ('CLIP', 'CLIP')
        comp.width, comp.height = (0, 2)
        comp.position = (-1, 0, 0.9)
        component.back.addChild(comp, 'filler')
        comp = GUI.Text(u'', 'RobotoCondensedRegular_16.font')
        comp.colour = cc.active_rgba
        comp.wm, comp.hm = ('CLIP', 'CLIP')
        comp.ha, comp.va = ('CENTER', 'CENTER')
        comp.hpm, comp.vpm = ('CLIP', 'PIXEL')
        comp.width, comp.height = (2, 2)
        comp.position = (0, component.back.height / 2 + 1, 0.5)
        component.back.addChild(comp)

    def _update_content(self):
        if self._value[1] != 0:
            self.component.back.filler.width = float(self._value[0]) / self._value[1] * 2

    def set_value(self, value):
        self._value = value
        self._update_content()