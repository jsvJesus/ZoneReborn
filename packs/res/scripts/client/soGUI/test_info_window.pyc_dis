# Embedded file name: ./client/soGUI/test_info_window.py
import BWPersonality
import BigWorld
import GUI
import Helpers.PyGUI as PyGUI
import soGUI
from Helpers.PyGUI import PyGUIEvent
from Helpers.PyGUI.PyGUIBase import PyGUIBase
from soGUI.soButton import soButton
from soGUI.soTextField import soTextField3
from Helpers.PyGUI.DraggableComponent import DraggableComponent

class TestWindow(PyGUIBase, DraggableComponent):
    factoryString = 'soGUI.TestWindow'

    def __init__(self, component):
        PyGUIBase.__init__(self, component)
        DraggableComponent.__init__(self, 0.1, 0.1, 0.1, xoffset=0.95, yoffset=0.9)
        component.script = self
        self.setupRoot()
        self.setupContent()

    def setupRoot(self):
        cmp = self.component
        cmp.horizontalPositionMode = cmp.verticalPositionMode = 'CLIP'
        cmp.horizontalAnchor = 'LEFT'
        cmp.verticalAnchor = 'TOP'
        cmp.widthMode = cmp.heightMode = 'PIXEL'
        cmp.width = 500
        cmp.height = 560
        cmp.position = (0.6, 0.99, 0.1)
        cmp.textureName = ''
        cmp.visible = True
        cmp.dragFocus = True
        self.component.mouseButtonFocus = 1

    def setupContent(self):
        cmp = GUI.Simple('soGUI/white.dds')
        cmp.horizontalPositionMode = cmp.verticalPositionMode = 'PIXEL'
        cmp.widthMode = cmp.heightMode = 'PIXEL'
        cmp.horizontalAnchor = 'LEFT'
        cmp.verticalAnchor = 'TOP'
        cmp.colour = (10, 11, 10, 250)
        cmp.materialFX = 'BLEND'
        cmp.width = self.component.width
        cmp.height = self.component.height
        cmp.position = (0, 0, 1.0)
        self.component.addChild(cmp, 'wndFrame')
        btn = soButton(GUI.Window())
        btn.initVSC('soGUI/elements/closeBox.xml')
        cmp = btn.component
        cmp.horizontalPositionMode = cmp.verticalPositionMode = 'PIXEL'
        cmp.widthMode = cmp.heightMode = 'PIXEL'
        cmp.horizontalAnchor = 'RIGHT'
        cmp.verticalAnchor = 'TOP'
        cmp.colour = (255, 255, 255, 255)
        cmp.materialFX = 'BLEND'
        cmp.width = 18
        cmp.height = 18
        cmp.position = (self.component.width - 6, 6, 0.2)
        btn.onClick = self.onCloseBox
        self.component.addChild(cmp, 'closeBox')
        btn.onBound()
        btn._updateVisualState()
        self.txt = GUI.Text('text')
        self.txt.horizontalAnchor = 'LEFT'
        self.txt.verticalAnchor = 'TOP'
        self.txt.verticalPositionMode = 'PIXEL'
        self.txt.horizontalPositionMode = 'PIXEL'
        self.txt.font = 'RobotoCondensedRegular_14.font'
        self.txt.colour = (255, 205, 149, 245)
        self.txt.position = (20, 40, 0.0)
        self.txt.visible = True
        self.txt.multiline = True
        self.component.addChild(self.txt, 'txt')

    def handleMouseButtonEvent(self, comp, event):
        return DraggableComponent.handleMouseButtonEvent(self, comp, event)

    def show(self):
        if self.component.parent is None:
            BWPersonality.GUICore.worldLayer.addChild(self.component, 'TestWindow')
        self.component.visible = True
        return

    def hide(self):
        self.component.visible = False
        BWPersonality.GUICore.setBestCursor()
        BigWorld.cancelCallback(self._timerID)

    def setText(self, text):
        self.component.txt.text = text

    def onCloseBox(self):
        self.hide()