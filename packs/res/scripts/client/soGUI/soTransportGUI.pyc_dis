# Embedded file name: ./client/soGUI/soTransportGUI.py
import GUI
from Helpers.PyGUI import PyGUIBase
from soGUI.GUIPositions import GUIPositions
from Helpers.PyGUI.DraggableComponent import DraggableComponent

class soTransportGUI(PyGUIBase, GUIPositions, DraggableComponent):
    factoryString = 'soGUI.soTransportGUI'

    def __init__(self):
        PyGUIBase.__init__(self, self._create_component())
        DraggableComponent.__init__(self, True, True, True, 0, 0)
        GUIPositions.__init__(self)
        self.component.script = self

    def _create_component(self):
        comp = GUI.Window('textures/default/white.dds')
        comp.colour = (0, 0, 0, 100)
        comp.materialFX = 'BLEND'
        comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
        comp.width, comp.height = (300, 100)
        comp.visible = False
        base_component = comp
        comp = GUI.Text('')
        comp.multiline = True
        comp.horizontalAnchor, comp.verticalAnchor = ('LEFT', 'TOP')
        comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
        comp.position = (16, 16, 0.1)
        comp.colour = (146, 146, 146, 255)
        comp.font = 'RobotoCondensedRegular_16.font'
        base_component.addChild(comp, 'text')
        return base_component

    def handleMouseButtonEvent(self, comp, event):
        return DraggableComponent.handleMouseButtonEvent(self, comp, event)

    def stopDragging(self):
        GUIPositions.stopDragging(self)
        DraggableComponent.stopDragging(self)

    def set_text(self, text):
        self.component.text.text = text

    def show(self):
        if self.component.visible:
            return
        GUIPositions.show(self)
        self.component.visible = True

    def reset_position(self):
        """ \xd1\x81\xd0\xb1\xd1\x80\xd0\xb0\xd1\x81\xd1\x8b\xd0\xb2\xd0\xb0\xd0\xb5\xd1\x82 \xd1\x81\xd0\xbe\xd1\x85\xd1\x80\xd0\xb0\xd0\xbd\xd1\x91\xd0\xbd\xd0\xbd\xd1\x8b\xd0\xb5 \xd0\xbf\xd0\xbe\xd0\xbb\xd0\xbe\xd0\xb6\xd0\xb5\xd0\xbd\xd0\xb8\xd1\x8f \xd0\xbe\xd0\xba\xd0\xbe\xd0\xbd """
        self.component.position = (0, 0, 0)

    def hide(self):
        self.component.text.text = u''
        self.component.visible = False

    @property
    def visible(self):
        return self.component.visible