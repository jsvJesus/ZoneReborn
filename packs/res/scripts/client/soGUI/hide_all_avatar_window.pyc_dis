# Embedded file name: ./client/soGUI/hide_all_avatar_window.py
import BigWorld
import GUI
import BWPersonality
from Helpers.PyGUI import PyGUIBase
from soGUI.soButton import soButton
from Helpers.PyGUI.DraggableComponent import DraggableComponent
from Localization import lc
from soGUI.GUIPositions import GUIPositions

class HideAllAvatarWindow(PyGUIBase, GUIPositions, DraggableComponent):
    factoryString = 'HideAllAvatarWindow'

    def __init__(self, component):
        PyGUIBase.__init__(self, component)
        self.component.script = self
        DraggableComponent.__init__(self, True, True, True, xoffset=0, yoffset=0)
        GUIPositions.__init__(self)
        cmp = self.component
        cmp.horizontalPositionMode = cmp.verticalPositionMode = 'CLIP'
        cmp.horizontalAnchor = 'RIGHT'
        cmp.verticalAnchor = 'TOP'
        cmp.widthMode = cmp.heightMode = 'PIXEL'
        cmp.colour = (100, 100, 100, 200)
        cmp.materialFX = 'BLEND'
        cmp.height = 30
        cmp.width = 250
        cmp.visible = True
        cmp.textureName = 'textures/default/white.dds'
        cmp.mouseButtonFocus = True
        cmp = GUI.Text(lc('PlayerAvatar.client.hide_all_wind_label'))
        cmp.horizontalPositionMode = cmp.verticalPositionMode = 'PIXEL'
        cmp.horizontalAnchor = 'LEFT'
        cmp.verticalAnchor = 'TOP'
        cmp.font = 'RobotoCondensedRegular_16.font'
        cmp.colour = (255, 255, 255, 255)
        cmp.materialFX = 'BLEND'
        cmp.position = (18, 6, 0.3)
        self.component.addChild(cmp, 'iLabel')
        btn = soButton(GUI.Window())
        cmp = btn.component
        btn.initVSC('soGUI/elements/button_1.xml')
        cmp.horizontalPositionMode = cmp.verticalPositionMode = 'PIXEL'
        cmp.widthMode = cmp.heightMode = 'PIXEL'
        cmp.horizontalAnchor = 'RIGHT'
        cmp.verticalAnchor = 'CENTER'
        cmp.label.font = 'RobotoCondensedRegular_16.font'
        cmp.label.text = u'X'
        cmp.colour = (255, 255, 255, 255)
        cmp.materialFX = 'BLEND'
        cmp.width = 25
        cmp.height = 25
        cmp.position = (self.component.width - 5, self.component.height / 2, 0.1)
        cmp.script.setDisabledState(False)
        cmp.script.onClick = self.unhideAvatars
        self.component.addChild(cmp, 'btn')
        btn.onBound()
        self.component.position = (1, -0.5, 0.2)

    def handleMouseButtonEvent(self, comp, event):
        return DraggableComponent.handleMouseButtonEvent(self, comp, event)

    def stopDragging(self):
        GUIPositions.stopDragging(self)
        DraggableComponent.stopDragging(self)

    def doReposition(self):
        DraggableComponent.restrictPosition(self)

    def show(self):
        self.component.visible = True
        BWPersonality.GUICore.worldLayer.delChild(self.factoryString)
        BWPersonality.GUICore.worldLayer.addChild(self.component, self.factoryString)
        GUIPositions.show(self)
        DraggableComponent.restrictPosition(self)

    def unhideAvatars(self):
        player = BigWorld.player()
        if hasattr(player, 'hideAllOtherAvatarByKey'):
            player.hideAllOtherAvatarByKey(False)
        self.hide()

    def hide(self):
        self.component.visible = False
        BWPersonality.GUICore.worldLayer.delChild(self.factoryString)