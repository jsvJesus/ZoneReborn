# Embedded file name: ./client/soGUI/craft_gui/ui_creator.py
import GUI
from soGUI.soButton import soButton
import BWPersonality
from functools import partial
from Localization import lc
import colorCodes as CC
from result_block import ResultBlock
from items_block import ItemsBlock
from kickstart_block import KickstartBlock
from recipes_block import RecipesBlock

def create_content(component):
    comp = GUI.Frame2('soGUI/frame/basic_frame_1.dds')
    comp.widthMode, comp.heightMode = ('CLIP', 'CLIP')
    comp.horizontalPositionMode, comp.verticalPositionMode = ('CLIP', 'CLIP')
    comp.horizontalAnchor, comp.verticalAnchor = ('CENTER', 'CENTER')
    comp.width, comp.height = (2, 2)
    comp.position = (0, 0, 0.9)
    comp.materialFX = 'SOLID'
    component.addChild(comp, 'background')
    comp = GUI.Text(lc('CraftWin.ui.caption'))
    comp.horizontalPositionMode, comp.verticalPositionMode = ('CLIP', 'PIXEL')
    comp.horizontalAnchor, comp.verticalAnchor = ('CENTER', 'TOP')
    comp.font = 'RobotoCondensedRegular_16.font'
    comp.position = (0, 5, 0.8)
    comp.colour = CC.active_rgba
    component.addChild(comp, 'caption')
    close_button = soButton(GUI.Window())
    comp = close_button.component
    comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
    comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
    comp.horizontalAnchor, comp.verticalAnchor = ('RIGHT', 'TOP')
    comp.materialFX = 'BLEND'
    comp.width, comp.height = (18, 18)
    comp.position = (component.width - 8, 8, 0.8)
    component.addChild(comp, 'closebutton')
    close_button.initVSC('soGUI/elements/closeBox.xml')
    close_button.onBound()
    close_button.setVisualState('normal')
    hint_button = soButton(GUI.Window())
    comp = hint_button.component
    comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
    comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
    comp.horizontalAnchor, comp.verticalAnchor = ('RIGHT', 'TOP')
    comp.materialFX = 'BLEND'
    comp.width, comp.height = (18, 18)
    comp.position = close_button.component.position + (-22, 0, 0)
    component.addChild(comp, 'hintbutton')
    hint_button.initVSC('soGUI/elements/helpBox.xml')
    hint_button.onBound()
    hint_button.setVisualState('normal')
    hint_button.onClick = partial(BWPersonality.GUICore.openGUIHelp, 'soCraftGUI')
    RecipesBlock(component)
    btn = soButton(GUI.Window())
    comp = btn.component
    btn.initVSC('soGUI/elements/button_1.xml')
    comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
    comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
    comp.horizontalAnchor, comp.verticalAnchor = ('CENTER', 'BOTTOM')
    comp.label.font = 'RobotoCondensedRegular_16.font'
    comp.label.text = lc('CraftWin.ui.apply')
    comp.width, comp.height = (180, 29)
    comp.position = (component.width - 150, 0, 0.5)
    btn.onBound()
    component.addChild(comp, 'applybutton')
    apply_comp = comp
    btn = soButton(GUI.Window())
    comp = btn.component
    btn.initVSC('soGUI/elements/button_1.xml')
    comp.horizontalPositionMode, comp.verticalPositionMode = apply_comp.horizontalPositionMode, apply_comp.verticalPositionMode
    comp.widthMode, comp.heightMode = apply_comp.widthMode, apply_comp.heightMode
    comp.horizontalAnchor, comp.verticalAnchor = apply_comp.horizontalAnchor, apply_comp.verticalAnchor
    comp.label.font = apply_comp.label.font
    comp.label.text = lc('CraftWin.ui.cancel')
    comp.width, comp.height = apply_comp.width, apply_comp.height
    comp.position = apply_comp.position
    btn.onBound()
    component.addChild(comp, 'cancelbutton')
    ResultBlock(component)
    ItemsBlock(component)
    KickstartBlock(component)