# Embedded file name: ./client/soGUI/soFillBar.py
import GUI
import BigWorld
from Helpers.PyGUI.PyGUIBase import PyGUIBase

class soFillBar(PyGUIBase):

    def __init__(self, component, size = (100, 5), position = (0, 0, 0), colour = (255, 255, 255, 255), value = 0):
        PyGUIBase.__init__(self, component)
        component.script = self
        self.value = value
        self.colour = colour
        self.setupRoot(size, position)
        self.setupControls()

    def setupRoot(self, size, position):
        cmp = self.component
        cmp.horizontalPositionMode = cmp.verticalPositionMode = 'PIXEL'
        cmp.widthMode = cmp.heightMode = 'PIXEL'
        cmp.horizontalAnchor = 'LEFT'
        cmp.verticalAnchor = 'TOP'
        cmp.position = position
        cmp.width = size[0]
        cmp.height = size[1]
        cmp.colour = (255, 255, 255, 255)
        cmp.textureName = ''
        cmp.materialFX = 'BLEND'

    def setupControls(self):
        cmp = GUI.Simple('textures/default/white.dds')
        cmp.horizontalPositionMode = cmp.verticalPositionMode = 'PIXEL'
        cmp.horizontalAnchor = 'LEFT'
        cmp.verticalAnchor = 'TOP'
        cmp.widthMode = cmp.heightMode = 'CLIP'
        cmp.colour = (10, 11, 10, 255)
        cmp.materialFX = 'BLEND'
        cmp.position = (0.0, 0.0, 1.0)
        cmp.width = 2.0
        cmp.height = 2
        self.component.addChild(cmp, 'background')
        cmp = GUI.Simple('textures/default/white.dds')
        cmp.horizontalPositionMode = cmp.verticalPositionMode = 'PIXEL'
        cmp.horizontalAnchor = 'LEFT'
        cmp.verticalAnchor = 'TOP'
        cmp.widthMode = 'PIXEL'
        cmp.heightMode = 'CLIP'
        cmp.colour = self.colour
        cmp.materialFX = 'BLEND'
        cmp.position = (0.0, 0.0, 1.0)
        cmp.width = self._getSize()
        cmp.height = 2.0
        self.component.addChild(cmp, 'valueItem')

    def _getSize(self):
        return self.component.width * self.value / 100.0

    def setColour(self, colour):
        self.component.valueItem.colour = colour

    def setValue(self, value):
        self.value = value
        self.component.valueItem.width = self._getSize()