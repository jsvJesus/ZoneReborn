# Embedded file name: ./client/soGUI/crosshairLookAround.py
import BWPersonality
import GUI
from Helpers.PyGUI import PyGUIBase
from Localization import lc

class CrosshairLookAround(PyGUIBase):
    factoryString = 'soGUI.CrosshairLookAround'

    def __init__(self, component):
        PyGUIBase.__init__(self, component)
        component.script = self
        self.setupRoot()
        self.setupContent()

    def setupRoot(self):
        cmp = self.component
        cmp.horizontalPositionMode = cmp.verticalPositionMode = 'CLIP'
        cmp.widthMode = cmp.heightMode = 'CLIP'
        cmp.horizontalAnchor = cmp.verticalAnchor = 'CENTER'
        cmp.colour = (255, 255, 255, 255)
        cmp.materialFX = 'BLEND'
        cmp.width = cmp.height = 2.0
        cmp.position = (0.0, 0.0, 1.5)
        cmp.visible = False

    def setupContent(self):
        cmp = GUI.Simple('soGUI/white.dds')
        cmp.widthMode = cmp.heightMode = 'PIXEL'
        cmp.horizontalAnchor = 'CENTER'
        cmp.verticalAnchor = 'CENTER'
        cmp.materialFX = 'BLEND'
        cmp.colour = (255, 100, 100, 100)
        cmp.width = cmp.height = 6
        cmp.position = (0.0, 0.0, 0.1)
        self.component.addChild(cmp, 'heapCENTER')
        self.component.heapCENTER.visible = True
        cmp = GUI.Text(lc('PlayerAvatar.client.LOOK_AROUND'))
        cmp.font = 'RobotoCondensedRegular_20.font'
        cmp.horizontalPositionMode = 'LEGACY'
        cmp.verticalPositionMode = 'PIXEL'
        cmp.widthMode = cmp.heightMode = 'PIXEL'
        cmp.horizontalAnchor = 'CENTER'
        cmp.multiline = 1
        cmp.verticalAnchor = 'TOP'
        cmp.colour = (146, 146, 146, 255)
        cmp.materialFX = 'BLEND'
        cmp.position = (0.0, 10, 0.0)
        cmp.visible = 1
        self.component.addChild(cmp, 'textHelp')

    def show(self):
        if self.component.parent is None:
            BWPersonality.GUICore.worldLayer.delChild('CrosshairLookAround')
            BWPersonality.GUICore.worldLayer.addChild(self.component, 'CrosshairLookAround')
        self.component.visible = True
        return

    def hide(self):
        self.component.visible = False