# Embedded file name: ./client/soGUI/achivstab/achivstab.py
import GUI
from Helpers.PyGUI import PyGUIBase
from achivslist import Achivslist
from achivsprops import AchivsProps
from achivsfilter import AchivsFilter
from Localization import lc

class AchivsTab(PyGUIBase):

    def __init__(self, component):
        PyGUIBase.__init__(self, component)
        component.script = self
        comp = GUI.Window()
        comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
        comp.horizontalAnchor, comp.verticalAnchor = ('LEFT', 'TOP')
        comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
        comp.width, comp.height = (338, 516)
        comp.position = (0, 34, 0.9)
        component.addChild(comp, 'list')
        self._ach_list = Achivslist(comp)
        self._ach_list.on_achiv_selected = self._on_achiv_selected
        self._ach_list.on_updated_callback = self._on_achs_updated
        comp = GUI.Window()
        comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
        comp.horizontalAnchor, comp.verticalAnchor = ('LEFT', 'TOP')
        comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
        comp.width, comp.height = (248, 508)
        comp.position = (334, 41, 0.9)
        comp.materialFX = 'BLEND'
        comp.colour = (80, 80, 120, 128)
        component.addChild(comp, 'props')
        self._ach_props = AchivsProps(comp)
        comp = GUI.Window()
        comp.horizontalPositionMode, comp.verticalPositionMode = ('CLIP', 'PIXEL')
        comp.horizontalAnchor, comp.verticalAnchor = ('RIGHT', 'BOTTOM')
        comp.widthMode, comp.heightMode = ('CLIP', 'PIXEL')
        comp.width, comp.height = (2.0, 40)
        comp.position = (1.0, 577, 0.9)
        comp.materialFX = 'BLEND'
        comp.colour = (80, 80, 120, 128)
        component.addChild(comp, 'filter')
        self._filter = AchivsFilter(comp)
        self._filter.on_value_changed_callback = self._on_filter_changed
        comp = GUI.Text()
        comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
        comp.horizontalAnchor, comp.verticalAnchor = ('LEFT', 'TOP')
        comp.position = (4, 14, 0.5)
        comp.font = 'RobotoCondensedRegular_14.font'
        component.addChild(comp, 'counter')

    def _on_achs_updated(self, got_count, total_count):
        self.component.counter.text = lc('AchivGui.countstats').format(got_count, total_count)

    def _on_filter_changed(self, value):
        self._ach_list.set_filter(value)

    def _on_achiv_selected(self, ach_id):
        self._ach_props.set_ach(ach_id)
        self._filter.deselect()

    def on_show(self):
        self._ach_list.on_show()
        self._ach_props.on_show()
        self._filter.deselect()

    def on_hide(self):
        self._ach_list.on_hide()
        self._ach_props.on_hide()
        self._filter.deselect()