# Embedded file name: ./client/soGUI/achivstab/achivsfilter.py
import GUI
from Helpers.PyGUI import PyGUIBase
from soGUI.soEditField import soEditBox

class AchivsFilter(PyGUIBase):

    def __init__(self, component):
        PyGUIBase.__init__(self, component)
        self.component.script = self
        value = soEditBox(GUI.Window(), width=248, height=18)
        value.onValueChange = self._on_value_changed
        comp = value.component
        comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
        comp.horizontalAnchor, comp.verticalAnchor = ('LEFT', 'TOP')
        comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
        comp.position = (69, 8, 0.3)
        self.component.addChild(comp, 'value')
        comp = GUI.Simple('soGUI/frame/search.dds')
        comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
        comp.width, comp.height = comp.texture.width, comp.texture.height
        comp.horizontalAnchor, comp.verticalAnchor = ('CENTER', 'TOP')
        comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
        comp.position = (37, 8, 0.5)
        self.component.addChild(comp)
        self.on_value_changed_callback = lambda value: None

    def deselect(self):
        self.component.value.script.activate(False)

    def _on_value_changed(self, by_user):
        value = self.component.value.script.getRaw()
        self.on_value_changed_callback(value)