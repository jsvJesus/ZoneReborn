# Embedded file name: ./client/soGUI/weapon_modification/fade_wind.py
import BigWorld
import GUI
from Helpers.PyGUI.PyGUIBase import PyGUIBase

class FadeWindow(PyGUIBase):

    def __init__(self, component, parent):
        PyGUIBase.__init__(self, component)
        component.script = self
        self.setupRoot()

    def setupRoot(self):
        W, H = BigWorld.screenSize()
        cmp = self.component
        cmp.horizontalPositionMode = cmp.verticalPositionMode = 'CLIP'
        cmp.textureName = 'textures/default/white.dds'
        cmp.widthMode = cmp.heightMode = 'CLIP'
        cmp.horizontalAnchor = 'CENTER'
        cmp.verticalAnchor = 'CENTER'
        cmp.width = cmp.height = 2.0
        cmp.position = (0, 0, 0.0)
        cmp.colour = (0, 0, 0, 150)
        cmp.materialFX = 'BLEND'
        cmp.focus = 1
        cmp.crossFocus = 1
        cmp.dragFocus = 1
        cmp.moveFocus = 1
        cmp.dropFocus = 1
        cmp.mouseButtonFocus = 1
        cmp.visible = False
        cmp = GUI.Simple('soGUI/maps/loadingScreen/loadingCircle/loadingCircle.texanim')
        cmp.horizontalPositionMode = 'PIXEL'
        cmp.verticalPositionMode = 'CLIP'
        cmp.horizontalAnchor = 'LEFT'
        cmp.verticalAnchor = 'TOP'
        cmp.widthMode = cmp.heightMode = 'PIXEL'
        cmp.colour = (150, 150, 150, 220)
        cmp.materialFX = 'BLEND'
        cmp.height = 41
        cmp.width = 41
        cmp.position = (5, 0, 0.01)
        self.component.addChild(cmp, 'darkScreenLogo')

    def show(self, x_pos = None):
        if x_pos is not None:
            self.component.darkScreenLogo.position.x = x_pos
        self.component.visible = True
        return

    def hide(self):
        self.component.visible = False

    def handleDropEvent(self, cmp, dropped):
        return True

    def handleDragStartEvent(self, *e):
        return True

    def handleDragStopEvent(self, *e):
        return True

    def handleMouseEvent(self, component, event):
        return True

    def handleMouseButtonEvent(self, comp, event):
        return True