# Embedded file name: ./client/soGUI/news_ulits_flash.py


def convert_news(text):
    if type(text) == unicode:
        text = text.encode('utf-8', 'ignore')
    text = text.replace('  \t\t', '')
    text = text.replace('  \t', '')
    return text