# Embedded file name: ./client/soGUI/downloadGUI.py
import BigWorld
import os
import Math
import GUI
import traceback
import BWPersonality
from Localization import lc, localization_object
from Helpers.PyGUI.PyGUIBase import PyGUIBase

class DownloadGUI(PyGUIBase):

    def __init__(self, component):
        PyGUIBase.__init__(self, component)
        component.script = self
        self.setupRoot()
        component.mouseButtonFocus = True
        component.crossFocus = True
        component.focus = 1
        component.moveFocus = 1
        component.dragFocus = 1

    def handleMouseEvent(self, component, event):
        return True

    def handleMouseButtonEvent(self, component, event):
        return True

    def handleMouseEnterEvent(self, component):
        return True

    def handleMouseLeaveEvent(self, component):
        return True

    def handleMouseLeaveEvent(self, component):
        return True

    def setupRoot(self):
        W, H = BigWorld.screenSize()
        cmp = self.component
        cmp.horizontalPositionMode = cmp.verticalPositionMode = 'CLIP'
        cmp.widthMode = cmp.heightMode = 'CLIP'
        cmp.horizontalAnchor = 'CENTER'
        cmp.verticalAnchor = 'CENTER'
        cmp.width = cmp.height = 20
        cmp.position = (0, 0, 0.9)
        cmp.colour = (255, 255, 255, 100)
        cmp.materialFX = 'BLEND'
        cmp.visible = True
        cmp.focus = 0
        cmp.crossFocus = 0
        cmp.dragFocus = False
        cmp.moveFocus = 1
        cmp.dropFocus = False
        cmp = GUI.Simple('textures/default/black.dds')
        cmp.horizontalPositionMode = cmp.verticalPositionMode = 'CLIP'
        cmp.widthMode = cmp.heightMode = 'CLIP'
        cmp.horizontalAnchor = cmp.verticalAnchor = 'CENTER'
        cmp.colour = (255, 255, 255, 255)
        cmp.materialFX = 'BLEND'
        cmp.width = cmp.height = 2.0
        cmp.position = (0.0, 0.0, 0.9)
        self.component.addChild(cmp, 'fon')
        cmp = GUI.Text('text.')
        cmp.font = 'RobotoCondensedRegular_18.font'
        cmp.colour = (255, 215, 255, 255)
        cmp.materialFX = 'BLEND'
        cmp.horizontalPositionMode = cmp.verticalPositionMode = 'CLIP'
        cmp.verticalAnchor = 'TOP'
        cmp.horizontalAnchor = 'CENTER'
        cmp.position = (0, 0, 0.1)
        self.component.addChild(cmp, 'txt')

    def show(self, msg = ''):
        if self.component.parent is None:
            BWPersonality.GUICore.menuLayer.addChild(self.component, 'download_gui')
        self.component.visible = 1
        self.component.txt.text = msg
        return

    def hide(self):
        self.component.visible = 0