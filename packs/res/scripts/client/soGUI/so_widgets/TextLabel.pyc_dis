# Embedded file name: ./client/soGUI/so_widgets/TextLabel.py
import GUI

class TextLabel:
    factoryString = 'soGUI.TextLabel'

    def __init__(self, h_anchor, v_anchor, position, font = 'RobotoCondensedRegular_16.font', text = '', colour = (255, 255, 255, 255)):
        cmp = GUI.Text('')
        cmp.horizontalPositionMode, cmp.verticalPositionMode = ('PIXEL', 'PIXEL')
        cmp.widthMode, cmp.heightMode = ('PIXEL', 'PIXEL')
        cmp.horizontalAnchor, cmp.verticalAnchor = h_anchor, v_anchor
        cmp.font = font
        cmp.text = text
        cmp.colour = colour
        cmp.position = position
        self.component = cmp