# Embedded file name: ./scripts/client/soGUI/emblemWindow.py
import BigWorld
import BWPersonality
import os
import UCManager
import DyeConstant
import GUI
import random
import traceback
from Localization import lc, localization_object
from Helpers.PyGUI.PyGUIBase import PyGUIBase
from soGUI.soButton import soButton, soButtonWTT
from soDraggableComponent import soDraggableComponent
from soGUI.soEditField import soEditField2
from soGUI.soEditField import soEditBox
from soGUI.soEditField import soEditBox2
import gui_jokes
import openFileDialog
from soGUI.soTextField import soTextField3

class EmblemWindow(PyGUIBase):
    factoryString = 'soGUI.EmblemWindow'

    def __init__(self, component, parent):
        PyGUIBase.__init__(self, component)
        self._fullpath = None
        self.parent = parent
        self._uppath = None
        component.script = self
        self.setupRoot()
        self.setupContent()
        return

    def setupRoot(self):
        cmp = self.component
        cmp.horizontalPositionMode = cmp.verticalPositionMode = 'PIXEL'
        cmp.mouseButtonFocus = 1
        cmp.widthMode = cmp.heightMode = 'PIXEL'
        cmp.horizontalAnchor = 'LEFT'
        cmp.verticalAnchor = 'TOP'
        cmp.colour = (255, 255, 255, 255)
        cmp.materialFX = 'BLEND'
        sW, sH = BigWorld.screenSize()
        self.width = cmp.width = 335
        self.height = cmp.height = 392
        cmp = GUI.Frame2('soGUI/maps/Controls/msgBox/windowFrame.tga')
        cmp.horizontalPositionMode = cmp.verticalPositionMode = 'CLIP'
        cmp.widthMode = cmp.heightMode = 'CLIP'
        cmp.horizontalAnchor = cmp.verticalAnchor = 'CENTER'
        cmp.colour = (255, 255, 255, 255)
        cmp.materialFX = 'BLEND'
        cmp.width = cmp.height = 2.0
        cmp.position = (0.0, 0.0, 0.9)
        self.component.addChild(cmp, 'dFrame')
        txt = soTextField3(GUI.Window(), width=300, height=170, textWidth=-1, hScroll=False, vScroll=True, hideScroll=False, borderWidth=0, vOffset=2, hOffset=2, textureless=0, autosize=0)
        cmp = txt.component
        cmp.horizontalPositionMode = cmp.verticalPositionMode = 'PIXEL'
        cmp.horizontalAnchor = 'LEFT'
        cmp.verticalAnchor = 'TOP'
        cmp.position = (20, 120, 0.2)
        self.component.addChild(cmp, 'eula')
        txt.onBound()
        txt.setText(lc('colourItemWindow.EULA_UPLOAD_EMBLEM'))

    def setupContent(self):
        self._texture_noimg = BigWorld.PyTextureProvider('soGUI/maps/ucsdata/noimg.png')
        cmp = GUI.Simple('soGUI/maps/ucsdata/noimg.png')
        cmp.horizontalPositionMode = cmp.verticalPositionMode = 'PIXEL'
        cmp.horizontalAnchor = 'LEFT'
        cmp.verticalAnchor = 'TOP'
        cmp.widthMode = cmp.heightMode = 'PIXEL'
        cmp.width = 64
        cmp.height = 64
        cmp.colour = (255, 255, 255, 255)
        cmp.materialFX = 'BLEND'
        cmp.position = (10, 40, 0.1)
        cmp.visible = 1
        self.component.addChild(cmp, 'loadedTexture')
        bOpen = soButton(GUI.Window())
        bOpen.initVSC('soGUI/visual_styles/defaultBtn.xml')
        cmp = bOpen.component
        cmp.horizontalPositionMode = cmp.verticalPositionMode = 'PIXEL'
        cmp.widthMode = cmp.heightMode = 'PIXEL'
        cmp.horizontalAnchor = 'LEFT'
        cmp.verticalAnchor = 'BOTTOM'
        cmp.colour = (255, 255, 255, 255)
        cmp.materialFX = 'BLEND'
        cmp.width = 100
        cmp.height = 29
        cmp.label.text = 'open...'
        cmp.position = (100, 100, 0.2)
        self.component.addChild(cmp, 'bOpen')
        bOpen.onClick = self.openFile
        bOpen.onBound()
        bOpen._updateVisualState()
        cmp = GUI.Text('file: ')
        cmp.font = 'ruRU_calibri_default.font'
        cmp.colour = (255, 255, 255, 255)
        cmp.materialFX = 'BLEND'
        cmp.horizontalPositionMode = cmp.verticalPositionMode = 'PIXEL'
        cmp.verticalAnchor = 'TOP'
        cmp.horizontalAnchor = 'LEFT'
        cmp.width = 100
        cmp.height = 20
        cmp.position = (100, 30, 0.0)
        cmp.visible = True
        self.component.addChild(cmp, 'fname')
        cmp = GUI.Text('size:')
        cmp.font = 'ruRU_calibri_default.font'
        cmp.colour = (255, 255, 255, 255)
        cmp.materialFX = 'BLEND'
        cmp.horizontalPositionMode = cmp.verticalPositionMode = 'PIXEL'
        cmp.verticalAnchor = 'TOP'
        cmp.horizontalAnchor = 'LEFT'
        cmp.width = 100
        cmp.height = 20
        cmp.position = (100, 48, 0.0)
        cmp.visible = True
        self.component.addChild(cmp, 'fsize')
        cmp = GUI.Text(lc('colourItemWindow.GOLD_COST').format(DyeConstant.COST_UPLOAD))
        cmp.font = 'ruRU_calibri_default.font'
        cmp.colour = (255, 215, 0, 255)
        cmp.materialFX = 'BLEND'
        cmp.horizontalPositionMode = cmp.verticalPositionMode = 'PIXEL'
        cmp.verticalAnchor = 'TOP'
        cmp.horizontalAnchor = 'LEFT'
        cmp.width = 100
        cmp.height = 20
        cmp.position = (12, self.height - 60, 0.0)
        cmp.visible = True
        self.component.addChild(cmp, 'fCost')
        bUpload = soButton(GUI.Window())
        bUpload.initVSC('soGUI/visual_styles/defaultBtn.xml')
        cmp = bUpload.component
        cmp.horizontalPositionMode = cmp.verticalPositionMode = 'PIXEL'
        cmp.widthMode = cmp.heightMode = 'PIXEL'
        cmp.horizontalAnchor = 'LEFT'
        cmp.verticalAnchor = 'BOTTOM'
        cmp.colour = (255, 255, 255, 255)
        cmp.materialFX = 'BLEND'
        cmp.width = 100
        cmp.height = 29
        cmp.label.text = lc('colourItemWindow.BTN_UPLOAD')
        cmp.position = (10, self.height - 10, 0.2)
        self.component.addChild(cmp, 'bUpload')
        bUpload.onClick = self.upload
        bUpload.onBound()
        bUpload._updateVisualState()
        self._open_wind = openFileDialog.OpenFileDialog(GUI.Window())

    def handleMouseButtonEvent(self, component, event):
        return True

    def openFile(self):
        self._open_wind.show(self.onOpenFile)

    def set_file_path(self, path):
        self.onOpenFile(path)

    def onOpenFile(self, fullpath):
        if not fullpath:
            return
        else:
            self._fullpath = fullpath
            self._uppath = None
            self.component.fname.text = 'File: '
            self.component.fsize.text = 'Size: '
            self.component.loadedTexture.texture = self._texture_noimg
            self._uploadtexture = None
            succes, tmppath, errormsg = UCManager.ucmanager.converToDDS(fullpath)
            if not succes:
                gui_jokes.askUserOk(u'ERROR', errormsg)
                return
            self._uppath = tmppath
            txtr = BigWorld.PyTextureProvider('../' + self._uppath, 1)
            strsize = '%sx%s' % (txtr.width, txtr.height)
            if txtr.height > 64 or txtr.width > 64:
                self._uppath = None
                self.component.loadedTexture.texture = self._texture_noimg
                gui_jokes.askUserOk(lc('colourItemWindow.ERROR'), lc('colourItemWindow.ERROR_SIZE').format(strsize))
                return
            self._uploadtexture = txtr
            self.component.loadedTexture.texture = self._uploadtexture
            self.component.fname.text = 'File: ' + fullpath[:6] + ' \\' + os.path.basename(fullpath)
            self.component.fsize.text = u'Size: ' + strsize
            self._uploadtexture = txtr
            BigWorld.addTextureFeed('tmp_set_emblem', txtr)
            self.parent.changeFeed('tmp_set_emblem')
            return

    def upload(self):
        print 'upload Emblem', [self._uppath]
        if not self._uppath:
            return
        pl = BigWorld.player()
        if not pl or not pl.__class__.__name__ == 'PlayerAvatar':
            return
        if pl.GoldCreditNumber < DyeConstant.COST_UPLOAD:
            gui_jokes.askUserOk(lc('colourItemWindow.ERROR'), lc('colourItemWindow.NOT_ENOUGH_GOLD'))
            return

        def callback(e):
            if e == gui_jokes.GuiHandler.OK:
                if UCManager.ucmanager.uploadImage(self._uppath, callbackUnlock=self.unlock, onUploadImage=self.onUploadImage):
                    self.lock()

        gui_jokes.askUserOk(lc('colourItemWindow.UPLOAD_EMBLEM'), lc('colourItemWindow.UPLOAD_EMBLEM_COST').format(DyeConstant.COST_UPLOAD), callback)

    def onUploadImage(self, res):
        print 'onUploadImage', [res]
        newImageID = int(res.strip())
        UCManager.download(newImageID)
        self.parent.onloadEmblemOk(newImageID)

    def lock(self):
        self.component.bOpen.script.setDisabledState(True)
        self.component.bUpload.script.setDisabledState(True)
        self.component.bOpen.script._updateVisualState()
        self.component.bUpload.script._updateVisualState()

    def unlock(self):
        self.component.bOpen.script.setDisabledState(False)
        self.component.bUpload.script.setDisabledState(False)
        self.component.bOpen.script._updateVisualState()
        self.component.bUpload.script._updateVisualState()