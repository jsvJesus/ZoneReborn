# Embedded file name: ./client/soGUI/emotions_gui/gear_toggle.py
import Keys
import GUI
from Helpers.PyGUI.PyGUIBase import Interceptor

class GearToggle(Interceptor):

    def __init__(self, on_down_click):
        comp = GUI.Window('soGUI/frame/grades/gear_inv.dds')
        comp.hpm, comp.vpm = ('PIXEL', 'PIXEL')
        comp.wm, comp.hm = ('PIXEL', 'PIXEL')
        comp.ha, comp.va = ('RIGHT', 'TOP')
        comp.materialFX = 'BLEND'
        comp.width, comp.height = comp.texture.width, comp.texture.height
        Interceptor.__init__(self, comp)
        self._on_down_click = on_down_click

    def handleMouseButtonEvent(self, comp, event):
        result = Interceptor.handleMouseButtonEvent(self, comp, event)
        if not event.isRepeatedEvent() and event.isKeyDown() and event.key == Keys.KEY_LEFTMOUSE:
            self._on_down_click()
        return result

    def set_highlighted(self, value):
        alpha = 255 if value else 150
        self.component.colour = (255,
         255,
         255,
         alpha)