# Embedded file name: ./client/soGUI/note_box.py
import GUI
from Helpers.PyGUI.PyGUIBase import PyGUIBase
from Helpers.PyGUI.DraggableComponent import DraggableComponent
import BWPersonality
from soGUI.soButton import soButton
from Localization import lc
from soGUI.soTextField import soTextField2
from soGUI.soTextField import soTxtFieldPropsStructure

class NoteBox(PyGUIBase, DraggableComponent):

    def __init__(self, description, item_name):
        component = GUI.Window()
        component.widthMode, component.heightMode = ('PIXEL', 'PIXEL')
        component.width, component.height = (600, 400)
        component.position = (0, 0, 0.1)
        PyGUIBase.__init__(self, component)
        DraggableComponent.__init__(self, True, True, True, xoffset=0, yoffset=0)
        component.script = self
        comp = GUI.Frame2('soGUI/frame/con_frame.dds')
        comp.horizontalPositionMode, comp.verticalPositionMode = ('CLIP', 'CLIP')
        comp.horizontalAnchor, comp.verticalAnchor = ('CENTER', 'CENTER')
        comp.materialFX = 'BLEND'
        comp.widthMode, comp.heightMode = ('CLIP', 'CLIP')
        comp.width, comp.height = (2, 2)
        comp.position = (0, 0, 0.9)
        component.addChild(comp)
        comp = GUI.Text(item_name)
        comp.font = 'RobotoCondensedRegular_16.font'
        comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
        comp.horizontalAnchor, comp.verticalAnchor = ('LEFT', 'CENTER')
        comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
        comp.width, comp.height = (50, 50)
        comp.position = (18, 15, 0.8)
        component.addChild(comp)
        comp = GUI.Window()
        btn = soButton(comp)
        btn.initVSC('soGUI/elements/closeBox.xml')
        comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
        comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
        comp.horizontalAnchor, comp.verticalAnchor = ('RIGHT', 'TOP')
        comp.width, comp.height = (18, 18)
        comp.position = (component.width - 8, 4, 0.2)
        component.addChild(comp)
        btn.onBound()
        btn.onClick = self.hide
        comp = GUI.Window()
        btn = soButton(comp)
        btn.initVSC('soGUI/elements/button_1.xml')
        comp.horizontalPositionMode, comp.verticalPositionMode = ('CLIP', 'PIXEL')
        comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
        comp.horizontalAnchor, comp.verticalAnchor = ('CENTER', 'BOTTOM')
        comp.width, comp.height = component.width - 54, 29
        comp.position = (0, component.height - 8, 0.5)
        comp.label.text = lc('soDialogueBox.soGUI.STRING_OK')
        component.addChild(comp)
        btn.onBound()
        btn.setVisualState('normal')
        btn.onClick = self.hide
        comp = GUI.Window('soGUI/frame/paint/blue.dds')
        comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
        comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
        comp.horizontalAnchor, comp.verticalAnchor = ('LEFT', 'TOP')
        comp.width, comp.height = component.width - 40, component.height - 90
        txt = soTextField2(comp, soTxtFieldPropsStructure(autoSize=False, height=comp.height, width=comp.width, vScroll=True, textureless=True))
        txt.onBound()
        txt.setText(u'\\c929292FF;' + description)
        comp.position = (20, 41, 0.1)
        component.addChild(comp)
        layer = BWPersonality.GUICore.worldLayer
        layer.delChild('quest_note')
        layer.addChild(component, 'quest_note')

    def hide(self):
        BWPersonality.GUICore.worldLayer.delChild('quest_note')

    def handleMouseButtonEvent(self, comp, event):
        return DraggableComponent.handleMouseButtonEvent(self, comp, event)