# Embedded file name: ./client/soGUI/promo_gui/promo_result_win.py
from win import Win, TextField, EventType as WinET
from Localization import lc
import GUI
from personal_win.content import ItemsView
from soGUI.UtilsGUI.PaintButton import PaintButton

class PromoResultWin(Win):

    def setup_content(self):
        Win.setup_content(self)
        self.create_caption(lc('PromoGui.promoresultwin.caption'))
        comp = GUI.Window()
        comp.horizontalAnchor, comp.verticalAnchor = ('CENTER', 'TOP')
        comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
        comp.position = (self.component.width * 0.5, 86, 0.5)
        tf = TextField(comp, width=self.component.width - 32, height=40, font='RobotoCondensedRegular_18.font', align='center')
        tf.setText(lc('PromoGui.promoresultwin.willget'))
        self.component.addChild(comp)
        comp = GUI.Window()
        comp.horizontalPositionMode, comp.verticalPositionMode = ('CLIP', 'PIXEL')
        comp.horizontalAnchor, comp.verticalAnchor = ('CENTER', 'TOP')
        comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
        comp.position = (0, 128, 0.5)
        self.component.addChild(comp)
        self._items_view = ItemsView(comp)
        width, height = (260, 46)
        comp = GUI.Window()
        comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
        comp.width, comp.height = width, height
        comp.horizontalAnchor, comp.verticalAnchor = ('CENTER', 'TOP')
        comp.horizontalPositionMode, comp.verticalPositionMode = ('CLIP', 'PIXEL')
        comp.position = (0, 100, 0.3)
        button0 = PaintButton(comp, visual_state='soGUI/frame/promo_gui/button2.xml')
        button0.text = lc('PromoGui.promoresultwin.ok')
        button0.on_click = self._on_ok_click
        self.component.addChild(comp, 'btn0')
        button0.do_reposition()

    def _align_content(self):
        pos_y = self._items_view.component.height + self._items_view.component.position[1]
        pos_y += 32
        self.component.btn0.position[1] = pos_y
        pos_y += self.component.btn0.height
        pos_y += 32
        self.component.height = pos_y

    def _on_ok_click(self):
        self.notify_listeners(WinET.OnRequestExit)

    def on_use_promocode(self, stritems):
        self._items_view.set_items(stritems)
        self._align_content()