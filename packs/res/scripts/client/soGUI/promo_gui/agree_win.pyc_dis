# Embedded file name: ./client/soGUI/promo_gui/agree_win.py
import GUI
from win import Win, EventType as WinET, TextField
from Localization import lc
from soGUI.UtilsGUI.ScrollBar import ScrollBar

class EventType:
    OnAgree, = ((r, 'promo.agree_win') for r in range(1))


class AgreeWin(Win):

    def setup_content(self):
        Win.setup_content(self)
        self.component.height = 618
        self.create_caption(lc('PromoGui.agreewin.caption'))
        agree_btn, disagree_btn = self.create_buttons()
        agree_btn.text = lc('PromoGui.agreewin.agree_btn')
        agree_btn.on_click = self._on_agree_click
        disagree_btn.text = lc('PromoGui.agreewin.disagree_btn')
        disagree_btn.on_click = self._on_disagree_click
        comp = GUI.Window()
        comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
        comp.horizontalAnchor, comp.verticalAnchor = ('LEFT', 'TOP')
        comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
        comp.width, comp.height = self.component.width - 26, 440
        comp.position = (26, 88, 0.5)
        self.component.addChild(comp)
        scroll_content_comp = comp
        comp = GUI.Window()
        comp.materialFX = 'SOLID'
        comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
        comp.horizontalAnchor, comp.verticalAnchor = ('CENTER', 'TOP')
        comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
        comp.position = (self.component.width / 2, 50, 0.5)
        scroll_comp = comp
        comp = GUI.Window()
        comp.horizontalAnchor, comp.verticalAnchor = ('LEFT', 'TOP')
        comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
        comp.position = (0, 0, 0.5)
        tf = TextField(comp, width=scroll_content_comp.width - 32, font='RobotoCondensedRegular_18.font')
        tf.setText(lc('PromoGui.agreewin.license'))
        scroll_comp.height = tf.component.height
        scroll_comp.addChild(tf.component)
        scroll = ScrollBar(scroll_content_comp, scroll_comp, scroll_content_comp, True)
        scroll_content_comp.bar.colour = (70, 69, 63, 255)
        scroll_content_comp.bar_stripe.colour = (70,
         69,
         63,
         127.5)
        scroll.set_line_pos(0.94)
        scroll.do_reposition()

    def _on_disagree_click(self):
        self.notify_listeners(WinET.OnRequestExit)

    def _on_agree_click(self):
        self.notify_listeners(EventType.OnAgree)