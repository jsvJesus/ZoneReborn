# Embedded file name: ./scripts/client/soGUI/soQuestRewardWin.py
from Helpers.PyGUI import PyGUIBase
from Helpers.PyGUI.DraggableComponent import DraggableComponent
from soGUI.soButton import soButton
import GUI
import BWPersonality
import Fractions
from Localization import lc
from soGUI.soItemView import soItemView
from soGUI.BarterWindowGUI import BarterPaySlot
import math

class QuestItemSlot(BarterPaySlot):

    def _added_item_text(self, data):
        return ' x {0}'.format(data['amount'])


class soQuestRewardWin(PyGUIBase, DraggableComponent):

    def __init__(self, component, manager, child_id):
        PyGUIBase.__init__(self, component)
        DraggableComponent.__init__(self, True, True, True, xoffset=0, yoffset=0)
        component.script = self
        self.manager = manager
        self.setupRoot()
        self.setupContent()
        self.child_id = child_id
        params = ['credits',
         'exp0',
         'exp1',
         'exp2',
         'reputation',
         'items']
        self.data = dict.fromkeys(params, None)
        return

    def setupRoot(self):
        cmp = self.component
        cmp.horizontalPositionMode = cmp.verticalPositionMode = 'CLIP'
        cmp.horizontalAnchor = 'CENTER'
        cmp.verticalAnchor = 'CENTER'
        cmp.widthMode = cmp.heightMode = 'PIXEL'
        cmp.width = 800
        cmp.height = 400
        cmp.position = (0.0, 0.0, 0.1)
        cmp.textureName = ''
        cmp.visible = False
        cmp.filterType = 'POINT'
        cmp.pixelSnap = False
        cmp.dragFocus = True
        self.component.mouseButtonFocus = 1

    def setupContent(self):
        cmp = GUI.Frame2('soGUI/maps/Controls/inventory/inventoryFrame2.tga')
        cmp.horizontalPositionMode = cmp.verticalPositionMode = 'PIXEL'
        cmp.widthMode = cmp.heightMode = 'PIXEL'
        cmp.horizontalAnchor = 'LEFT'
        cmp.verticalAnchor = 'TOP'
        cmp.colour = (255, 255, 255, 255)
        cmp.materialFX = 'BLEND'
        cmp.width = self.component.width
        cmp.height = self.component.height
        cmp.position = (0, 0, 1.0)
        self.component.addChild(cmp, 'wndFrame')
        parent = self.component.wndFrame
        btn = soButton(GUI.Window())
        btn.initVSC('soGUI/visual_styles/closeBox.xml')
        self.close_btn = btn.component
        self.close_btn.horizontalPositionMode = self.close_btn.verticalPositionMode = 'PIXEL'
        self.close_btn.widthMode = self.close_btn.heightMode = 'PIXEL'
        self.close_btn.horizontalAnchor = 'RIGHT'
        self.close_btn.verticalAnchor = 'TOP'
        self.close_btn.colour = (255, 255, 255, 255)
        self.close_btn.materialFX = 'BLEND'
        self.close_btn.width = 18
        self.close_btn.height = 18
        self.close_btn.position = (parent.position[0] + 30, 6, 0.2)
        btn.onClick = self.hide
        self.component.wndFrame.addChild(self.close_btn, 'closeBox')
        btn.onBound()
        btn._updateVisualState()
        parent = self.component.wndFrame
        self.text_frame = GUI.Frame2('soGUI/maps/Controls/TabletPC/questLog/doubleFrame.tga')
        self.text_frame.horizontalPositionMode = self.text_frame.verticalPositionMode = 'PIXEL'
        self.text_frame.widthMode = self.text_frame.heightMode = 'PIXEL'
        self.text_frame.horizontalAnchor = 'LEFT'
        self.text_frame.verticalAnchor = 'TOP'
        self.text_frame.colour = (255, 255, 255, 255)
        self.text_frame.materialFX = 'BLEND'
        self.text_frame.width = 200
        self.text_frame.height = parent.height - 70
        self.text_frame.position = (6, 32, 1.0)
        self.component.wndFrame.addChild(self.text_frame, 'textFrame')
        self.main_text = GUI.Text('')
        parent = self.component.wndFrame.textFrame
        self.main_text.horizontalPositionMode = self.main_text.verticalPositionMode = 'PIXEL'
        self.main_text.horizontalAnchor = 'LEFT'
        self.main_text.multiline = True
        self.main_text.width = 50
        self.main_text.verticalAnchor = 'TOP'
        self.main_text.font = 'ruRU_Cyrvetica_Extra_16i.font'
        self.main_text.colour = (175, 166, 112, 255)
        self.main_text.materialFX = 'BLEND'
        self.main_text.position = (parent.position[0] + 6, parent.position[1] + 6, 0.3)
        parent.addChild(self.main_text, 'descLabel')
        itemView = soItemView(GUI.Window(), QuestItemSlot, rowWidth=3, cols=3, rows=3, vScroll=True, hScroll=False, borderWidth=0)
        self.item_view_position = self.text_frame.position[0] + self.text_frame.width + 2
        self.item_view = itemView.component
        self.item_view.horizontalPositionMode = self.item_view.verticalPositionMode = 'PIXEL'
        self.item_view.horizontalAnchor = 'LEFT'
        self.item_view.verticalAnchor = 'TOP'
        self.item_view.position = (self.item_view_position, self.text_frame.position[1], 0.5)
        self.item_view.colour = (255, 255, 255, 255)
        self.item_view.materialFX = 'BLEND'
        self.component.wndFrame.addChild(self.item_view, 'itemView')
        itemView.onBound()
        itemView.setRestrictingComponent(None)
        itemView.setItems(None)
        parent = self.component.wndFrame
        self.btn_ok = soButton(GUI.Window())
        comp = self.btn_ok.component
        self.btn_ok.initVSC('soGUI/visual_styles/defaultBtn.xml')
        comp.horizontalPositionMode = comp.verticalPositionMode = 'PIXEL'
        comp.widthMode = comp.heightMode = 'PIXEL'
        comp.horizontalAnchor = 'LEFT'
        comp.verticalAnchor = 'TOP'
        comp.label.font = 'ruRU_calibri_default.font'
        comp.label.text = lc('GUI.QuestItems.OK_BUTTON')
        comp.colour = (255, 255, 255, 255)
        comp.materialFX = 'BLEND'
        comp.width = 62
        comp.height = 29
        comp.position = (parent.position[0] + 8, parent.height - 32, 0.5)
        self.component.wndFrame.addChild(comp, 'okButton')
        self.btn_ok.onBound()
        self.btn_ok.setVisualState('normal')
        self.btn_ok.onClick = self.hide
        return

    def get_main_text(self):
        text = u''
        if self.data['credits']:
            text += u'{0} {1}\n'.format(self.data['credits'], lc('GUI.QuestItems.RUBLES'))
        if self.data['exp0']:
            text += u'{0} {1}\n'.format(self.data['exp0'], lc('GUI.QuestItems.COMBAT_EXP'))
        if self.data['exp1']:
            text += u'{0} {1}\n'.format(self.data['exp1'], lc('GUI.QuestItems.SURVIVAL_EXP'))
        if self.data['exp2']:
            text += u'{0} {1}\n'.format(self.data['exp2'], lc('GUI.QuestItems.OTHER_EXP'))
        rep = self.data['reputation']
        if rep:
            text += u'\n'
            for key, value in rep.items():
                fraction_name = lc(Fractions.FractionNames[key])
                text += u'{0} {1}\n'.format(fraction_name, value)

        return text

    def show(self):
        if self.component.parent is None:
            BWPersonality.GUICore.worldLayer.addChild(self.component, 'questrewardWindow' + str(self.child_id))
        self.component.visible = True
        BWPersonality.GUICore.setBestCursor()
        return

    def merge_dict(self, dict1, dict2):
        """ \xd1\x81\xd0\xbb\xd0\xb8\xd0\xb2\xd0\xb0\xd0\xb5\xd1\x82 \xd1\x81\xd0\xbb\xd0\xbe\xd0\xb2\xd0\xb0\xd1\x80\xd0\xb8 dic1 \xd0\xb8 dict2 \xd1\x81\xd0\xba\xd0\xbb\xd0\xb0\xd0\xb4\xd1\x8b\xd0\xb2\xd0\xb0\xd0\xb8\xd1\x8f \xd0\xb8\xd1\x85 \xd0\xb7\xd0\xbd\xd0\xb0\xd1\x87\xd0\xb5\xd0\xbd\xd0\xb8\xd1\x8f """
        for key, value in dict2.items():
            if key not in dict1 or not dict1[key]:
                dict1[key] = value
                continue
            if isinstance(value, dict):
                self.merge_dict(dict1[key], value)
                continue
            dict1[key] += value

    def append_reward(self, reward):
        self.merge_dict(self.data, reward)
        self.update()

    def update(self):
        self.main_text.text = self.get_main_text()
        text_height = self.main_text.stringDimensions(self.main_text.text)[1]
        if self.main_text.text:
            self.text_frame.visible = True
            self.item_view.position[0] = self.item_view_position
        else:
            self.text_frame.visible = False
            self.item_view.position[0] = 1
        if self.data['items']:
            self.item_view.visible = True
            items_count = len(self.data['items'])
            min_rows_count = math.ceil(text_height / 64.0)
            rows_default = max(4, min_rows_count)
            cols_count = int(min(3, math.ceil(float(items_count) / rows_default)))
            rows_count = int(max(min_rows_count, int(math.ceil(float(items_count) / cols_count))))
            self.item_view.script.change_size(cols_count, rows_count)
            items = []
            for item_type, item_count in self.data['items'].items():
                items.append({'complexItemType': item_type,
                 'amount': item_count})

            self.item_view.script.setItems(items)
            self.component.width = self.item_view.position[0] + self.item_view.width + 2
        else:
            self.item_view.visible = False
            self.component.width = self.text_frame.width + 10
        frame_height = max(self.item_view.height, text_height)
        self.component.height = frame_height + self.item_view.position[1] + 38
        self.text_frame.height = frame_height
        self.component.wndFrame.width = self.component.width
        self.component.wndFrame.height = self.component.height
        self.btn_ok.component.position[0] = self.component.width * 0.5 - self.btn_ok.component.width * 0.5
        self.btn_ok.component.position[1] = self.component.height - 33
        self.close_btn.position[0] = self.component.width - 8

    def clear_data(self):
        for key in self.data:
            self.data[key] = None

        return

    def hide(self):
        self.component.visible = False
        self.manager.win_hidden(self)
        BWPersonality.GUICore.setBestCursor()

    def handleMouseButtonEvent(self, comp, event):
        return DraggableComponent.handleMouseButtonEvent(self, comp, event)