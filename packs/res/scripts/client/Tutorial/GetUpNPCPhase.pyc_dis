# Embedded file name: ./client/Tutorial/GetUpNPCPhase.py
from TriggerPhase import TriggerPhase
from Localization import lc

class GetUpNPCPhase(TriggerPhase):

    def __init__(self, global_index, actor):
        TriggerPhase.__init__(self, ['tutorial_getup'], global_index, actor)
        self.set_standard_text(lc('Tutorial.phases.PHASE_GETUP'))
        self.show_in_gui()

    def create_component(self):
        return self.create_standard_text()