# Embedded file name: ./client/Tutorial/FindHide.py
from Localization import lc
import Quest
from QuestPhase import QuestPhase

class FindHide(QuestPhase):

    def __init__(self, global_index, actor):
        QuestPhase.__init__(self, ((19633, Quest.STATE_CLOSED),), global_index, actor)
        self.set_standard_text(lc('Tutorial.phases.PHASE_15'))
        self.show_in_gui()

    def create_component(self):
        return self.create_standard_text()