# Embedded file name: ./client/Tutorial/GatherWoodPhase.py
import BWPersonality
from Localization import lc
from functools import partial
from SequentialPhase import SequentialPhase
from OpenUIPhase import OpenUIPhase, CHAT_TYPE
from PollPhase import PollPhase
from QuestPhase import QuestPhase
import Quest

class GatherWoodPhase(SequentialPhase):

    def __init__(self, global_index, actor):
        quest_id = 10231
        complete_quest = partial(QuestPhase, [(quest_id, Quest.STATE_CLOSED)], global_index, actor)
        SequentialPhase.__init__(self, [complete_quest], global_index, actor)

    def create_component(self):
        return self.create_standard_text()

    def phase_started(self, phase, phase_index):
        self.set_standard_text(lc('Tutorial.phases.PHASE_66_' + str(phase_index)))
        self.show_in_gui()