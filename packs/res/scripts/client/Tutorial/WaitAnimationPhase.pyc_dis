# Embedded file name: ./client/Tutorial/WaitAnimationPhase.py
from PollPhase import PollPhase

class WaitAnimationPhase(PollPhase):

    def __init__(self, global_index, actor):
        PollPhase.__init__(self, [self._check], global_index, actor)
        self._ignore_tick = True

    def _check(self):
        ignore_tick = self._ignore_tick
        self._ignore_tick = False
        if ignore_tick:
            return False
        return not self.actor._tutorial_manager._recorder.isNPCDemoPlayed()