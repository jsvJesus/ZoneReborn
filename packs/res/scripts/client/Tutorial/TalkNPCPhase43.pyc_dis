# Embedded file name: ./client/Tutorial/TalkNPCPhase43.py
import TutorialUtils
from Localization import lc
from SpeakNPCPhase import SpeakNPCPhase

class TalkNPCPhase43(SpeakNPCPhase):

    def __init__(self, actor):
        SpeakNPCPhase.__init__(self, TutorialUtils.TUTOR_NAME, SpeakNPCPhase.COMPLETE_ON_END, actor)

    def create_component(self):
        text = self.create_standard_text()
        self.set_standard_text(lc('Tutorial.phases.PHASE_43_0'))
        return text