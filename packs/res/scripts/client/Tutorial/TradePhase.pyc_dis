# Embedded file name: ./client/Tutorial/TradePhase.py
from Localization import lc
from GetItemsPhase import GetItemsPhase

class TradePhase(GetItemsPhase):

    def __init__(self, global_index, actor):
        GetItemsPhase.__init__(self, [2050], GetItemsPhase.INVENTORY_ITEMS, global_index, actor)
        self.show_in_gui()

    def create_component(self):
        text = self.create_standard_text()
        self.set_standard_text(lc('Tutorial.phases.PHASE_11_0'))
        return text