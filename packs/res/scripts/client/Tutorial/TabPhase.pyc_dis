# Embedded file name: ./client/Tutorial/TabPhase.py
import GUI
from Phase import Phase
from Localization import lc
from soGUI.soButton import soButton

class TabPhase(Phase):

    def __init__(self, *args, **kargs):
        Phase.__init__(self, *args, **kargs)
        self.show_in_gui()

    def create_component(self):
        container = GUI.Window()
        container.horizontalPositionMode, container.verticalPositionMode = ('CLIP', 'CLIP')
        container.horizontalAnchor, container.verticalAnchor = ('CENTER', 'CENTER')
        container.widthMode, container.heightMode = ('CLIP', 'CLIP')
        container.width, container.height = (2, 2)
        container.position = (0, 0, 0.5)
        text_comp = self.create_standard_text()
        self.set_standard_text(lc('Tutorial.phases.PHASE_1_0'))
        container.addChild(text_comp, 'text')
        ok_btn = soButton(GUI.Window())
        comp = ok_btn.component
        comp.horizontalPositionMode, comp.verticalPositionMode = ('CLIP', 'CLIP')
        comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
        comp.horizontalAnchor, comp.verticalAnchor = ('CENTER', 'BOTTOM')
        comp.width, comp.height = (130, 29)
        comp.position = (0, -0.7, 0.2)
        ok_btn.initVSC('soGUI/elements/button_1.xml')
        ok_btn.onBound()
        ok_btn.setVisualState('normal')
        ok_btn.onClick = self.phase_completed
        comp.label.text = lc('Tutorial.ui.ok_button')
        container.addChild(comp, 'button')
        return container