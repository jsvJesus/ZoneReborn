# Embedded file name: ./client/Tutorial/OpenUIPhase.py
import BWPersonality
from PollPhase import PollPhase
INVENTORY_TYPE, JOURNAL_TYPE, CHAT_TYPE, EQUIP_TYPE, SKILLS_TYPE, MODIFICATION_TYPE, MAP_TYPE = xrange(7)

class OpenUIPhase(PollPhase):

    def __init__(self, ui_type, *args, **kargs):
        PollPhase.__init__(self, [self._check], *args, **kargs)
        self._ui_type = ui_type

    def _check(self):
        if self._ui_type == INVENTORY_TYPE:
            return BWPersonality.GUICore.is_inventory_visible()
        if self._ui_type == JOURNAL_TYPE:
            return BWPersonality.GUICore.journal_win_visible()
        if self._ui_type == CHAT_TYPE:
            return BWPersonality.GUICore.is_chat_visible()
        if self._ui_type == EQUIP_TYPE:
            return BWPersonality.GUICore.is_equipment_visible()
        if self._ui_type == SKILLS_TYPE:
            return BWPersonality.GUICore.is_skills_visible()
        if self._ui_type == MODIFICATION_TYPE:
            return BWPersonality.GUICore.is_modification_visible()
        if self._ui_type == MAP_TYPE:
            return BWPersonality.GUICore.is_worldmap_visible()
        raise RuntimeError
        return False