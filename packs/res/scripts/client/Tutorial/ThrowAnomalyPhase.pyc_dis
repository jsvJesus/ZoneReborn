# Embedded file name: ./client/Tutorial/ThrowAnomalyPhase.py
from Localization import lc
from Phase import Phase

class ThrowAnomalyPhase(Phase):

    def __init__(self, global_index, actor):
        Phase.__init__(self, global_index, actor)
        self.set_standard_text(lc('Tutorial.phases.PHASE_24'))
        self.show_in_gui()

    def on_throwable_anomaly_hit(self, throwable_id, anomaly_id):
        self.phase_completed()

    def create_component(self):
        return self.create_standard_text()