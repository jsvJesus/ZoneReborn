# Embedded file name: ./client/Tutorial/TalkNpcQuest.py
from TalkNPCPhase import TalkNPCPhase

class TalkNpcQuest(TalkNPCPhase):

    def phase_started(self, phase, phase_index):
        TalkNPCPhase.phase_started(self, phase, phase_index)
        if phase_index == 0:
            self.actor.request_tutorial_quest(self.global_index, phase_index)