# Embedded file name: ./client/Tutorial/SlidePhase.py
from PollPhase import PollPhase
from Localization import lc

class SlidePhase(PollPhase):

    def __init__(self, global_index, actor):
        PollPhase.__init__(self, [self._check], global_index, actor)
        self.set_standard_text(lc('Tutorial.phases.PHASE_21'))
        self.show_in_gui()

    def create_component(self):
        return self.create_standard_text()

    def _check(self):
        return self.actor.lock_move_by_slide