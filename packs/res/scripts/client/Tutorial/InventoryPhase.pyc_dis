# Embedded file name: ./client/Tutorial/InventoryPhase.py
from Localization import lc
from OpenUIPhase import OpenUIPhase, INVENTORY_TYPE
from SequentialPhase import SequentialPhase
from SetQuickbarPhase import SetQuickbarPhase
from functools import partial

class InventoryPhase(SequentialPhase):

    def __init__(self, global_index, actor):
        open_inventory_phase = partial(OpenUIPhase, INVENTORY_TYPE, global_index, actor)
        quick_slot_phase = partial(SetQuickbarPhase, [400,
         475,
         810201,
         410075], global_index, actor)
        phases = [open_inventory_phase, quick_slot_phase]
        SequentialPhase.__init__(self, phases, global_index, actor)

    def create_component(self):
        return self.create_standard_text()

    def phase_started(self, phase, phase_index):
        self.set_standard_text(lc('Tutorial.phases.PHASE_17_' + str(phase_index)))
        self.show_in_gui()