# Embedded file name: ./client/Tutorial/BlockQuestPhase.py
from functools import partial
from Localization import lc
from SequentialPhase import SequentialPhase
from SpeakNPCPhase import SpeakNPCPhase
from QuestPhase import QuestPhase
import Quest

class BlockQuestPhase(SequentialPhase):

    def __init__(self, global_index, actor):
        quest_id_parent = 10232
        quest_id_child = 10233
        speak_phase = partial(SpeakNPCPhase, 'Fake_Charon', SpeakNPCPhase.COMPLETE_ON_END, global_index, actor)
        SequentialPhase.__init__(self, [speak_phase], global_index, actor)

    def create_component(self):
        return self.create_standard_text()

    def phase_started(self, phase, phase_index):
        self.set_standard_text(lc('Tutorial.phases.PHASE_8_' + str(phase_index)))
        self.show_in_gui()