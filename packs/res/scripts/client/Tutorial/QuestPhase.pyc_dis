# Embedded file name: ./client/Tutorial/QuestPhase.py
from PollPhase import PollPhase

class QuestPhase(PollPhase):
    """ conditions \xd0\xb2 \xd0\xb2\xd0\xb8\xd0\xb4\xd0\xb5: ((quest_id, status),)
            \xd0\xbf\xd0\xbe \xd1\x83\xd0\xbc\xd0\xbe\xd0\xbb\xd1\x87\xd0\xb0\xd0\xbd\xd0\xb8\xd1\x8e \xd0\xbf\xd1\x80\xd0\xbe\xd0\xb2\xd0\xb5\xd1\x80\xd1\x8f\xd0\xb5\xd1\x82 \xd0\xb2\xd1\x81\xd0\xb5 \xd1\x83\xd1\x81\xd0\xbb\xd0\xbe\xd0\xb2\xd0\xb8\xd1\x8f, \xd0\xbd\xd0\xbe \xd0\xbc\xd0\xbe\xd0\xb6\xd0\xbd\xd0\xbe \xd0\xb2\xd0\xba\xd0\xbb\xd1\x8e\xd1\x87\xd0\xb8\xd1\x82\xd1\x8c \xd0\xbf\xd1\x80\xd0\xbe\xd0\xb2\xd0\xb5\xd1\x80\xd0\xba\xd1\x83 \xd0\xbe\xd0\xb4\xd0\xbd\xd0\xbe\xd0\xb3\xd0\xbe \xd0\xbb\xd1\x8e\xd0\xb1\xd0\xbe\xd0\xb3\xd0\xbe
    """

    def __init__(self, conditions, *args, **kargs):
        self._conditions = conditions
        self._any_mode = False
        PollPhase.__init__(self, [self._check], *args, **kargs)
        quest_ids = [ c[0] for c in conditions ]
        self._statuses = {}
        self.actor.register_quest_listener(self, quest_ids)

    def switch_any_mode(self, value):
        self._any_mode = value

    def _check_status(self, quest_id, status):
        if quest_id not in self._statuses:
            return False
        return self._statuses[quest_id] == status

    def on_quest_state_changed(self, quest_id, state):
        self._statuses[quest_id] = state

    def _check(self):
        if self._any_mode:
            for quest_id, status in self._conditions:
                if self._check_status(quest_id, status):
                    return True

            return False
        else:
            for quest_id, status in self._conditions:
                if not self._check_status(quest_id, status):
                    return False

            return True

    def on_destroy(self):
        if self.destroyed:
            return
        self.actor.unregister_quest_listener(self)
        PollPhase.on_destroy(self)