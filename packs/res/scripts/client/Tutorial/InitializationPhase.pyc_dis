# Embedded file name: ./client/Tutorial/InitializationPhase.py
import BigWorld
from Phase import Phase

class InitializationPhase(Phase):

    def __init__(self, *args, **kargs):
        Phase.__init__(self, *args, **kargs)
        self.set_standard_text('')
        self.show_in_gui()
        BigWorld.callback(0, self.phase_completed)

    def create_component(self):
        return self.create_standard_text()