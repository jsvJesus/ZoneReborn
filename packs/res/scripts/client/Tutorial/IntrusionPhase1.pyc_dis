# Embedded file name: ./client/Tutorial/IntrusionPhase1.py
from SpeakNPCPhase import SpeakNPCPhase
from Localization import lc

class IntrusionPhase1(SpeakNPCPhase):

    def __init__(self, global_index, actor):
        SpeakNPCPhase.__init__(self, 'Fake_Charon', SpeakNPCPhase.COMPLETE_ON_END, global_index, actor)
        self.show_in_gui()

    def create_component(self):
        text = self.create_standard_text()
        self.set_standard_text(lc('Tutorial.phases.PHASE_12_0'))
        return text