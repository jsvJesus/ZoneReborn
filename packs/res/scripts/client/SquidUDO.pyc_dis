# Embedded file name: ./client/SquidUDO.py
import BigWorld
import DrawPrimitivesUtils
from draw_utils_extend import LinesHolder
from Squids.Constants import DEBUG

class SquidUDO(BigWorld.UserDataObject):

    def __init__(self):
        BigWorld.UserDataObject.__init__(self)
        self._lines = None
        if DEBUG:
            self._lines = LinesHolder(max_lines=0)
            DrawPrimitivesUtils.draw_cilinder(self.position, self.radius, 1, color=(0, 0, 255, 255), holder=self._lines, alwaysdraw=False)
        return

    def onLeaveWorld(self):
        if self._lines:
            self._lines.clear()