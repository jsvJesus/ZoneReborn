# Embedded file name: ./scripts/client/AutomaticSalute.py
import Math
import BigWorld
MODEL = 'models/props/ng/fireworks_launchbox_01_lod1.model'

class AutomaticSalute(BigWorld.Entity):
    """" client/AutomaticSalute """

    def __init__(self):
        self.height = 1.2
        BigWorld.Entity.__init__(self)
        self.name = u''

    def onLoadModel(self, model):
        if self.isDestroyed:
            return
        self.model = model
        self.height = Math.Matrix(self.model.bounds).get(1, 1) if model else 1.0

    def onEnterWorld(self, prereqs):
        BigWorld.fetchModelObstacle(MODEL, self.matrix, 0, self.onLoadModel)
        self.targetFullBounds = True