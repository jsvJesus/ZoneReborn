# Embedded file name: ./client/ClanWarehouse.py
import BigWorld
import BWPersonality

class ClanWarehouse:
    """" client/ClanWarehouse """

    def __init__(self):
        pass

    def set_storedClanItems(self, old_value):
        BigWorld.player().NeedToUpdateItemCache()

    def setNested_storedClanItems(self, path, oldValue):
        BigWorld.player().NeedToUpdateItemCache()

    def showClanManagerNPCGUI(self):
        BWPersonality.GUICore.showClanManagerNPCGUI(True, self.id)

    def request_warehouse_data1(self, first_index, amount):
        self.cell.request_warehouse_data2(first_index, amount)

    def got_warehouse_data2(self, items, index, total, money):
        BWPersonality.GUICore.clan_manager_win.got_warehouse_data3(items, index, total, money)

    def on_item_appended2(self):
        BWPersonality.GUICore.clan_manager_win.on_item_appended3()

    def on_item_removed2(self, item_index):
        BWPersonality.GUICore.clan_manager_win.on_item_removed3(item_index)

    def on_money_changed2(self, money):
        BWPersonality.GUICore.clan_manager_win.on_money_changed2(money)