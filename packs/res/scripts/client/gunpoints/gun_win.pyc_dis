# Embedded file name: ./client/gunpoints/gun_win.py
import GUI
from Helpers.PyGUI.PyGUIBase import DragWin
from heliutils.gui.heliinterface import MinigunBlock

class GunWin(DragWin):

    def __init__(self):
        comp = GUI.Window('soGUI/frame/paint/black.dds')
        comp.horizontalPositionMode, comp.verticalPositionMode = ('PIXEL', 'PIXEL')
        comp.horizontalAnchor, comp.verticalAnchor = ('LEFT', 'TOP')
        comp.widthMode, comp.heightMode = ('PIXEL', 'PIXEL')
        comp.width, comp.height = (240, 135)
        comp.position = (0, 165, 0.5)
        comp.colour = (255, 255, 255, 100)
        comp.materialFX = 'BLEND'
        DragWin.__init__(self, comp)
        comp.visible = False
        self._minigun_block = MinigunBlock(comp)

    @property
    def count(self):
        return self._count

    @count.setter
    def count(self, value):
        self._count = value
        self._minigun_block._set_count(self._count)

    def show(self, gun_id):
        self.count = 0
        self._gun_id = gun_id
        self.component.visible = True
        self._minigun_block._set_ratio(0)

    def update_temperature(self, temp_ratio):
        self._minigun_block._set_ratio(temp_ratio)

    def hide(self):
        self.component.visible = False

    @property
    def visible(self):
        return self.component.visible