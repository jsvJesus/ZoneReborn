# Embedded file name: ./common/hunt_utils.py


class OrderState:
    Common, NoSimple, NoMature, NoUndefined, HighKarma, Ordered, CoClan, LowKarma = xrange(1, 9)


class EventCodes:
    Registered, WantedChanged, OnlineWantedChanged, WantedKilledImprisoned, DonateWantedNotFound = xrange(1, 6)


class RemoveReason:
    Imprisoned, Timeout = range(2)


class UnregisterReason:
    Left, Reformed = range(2)


class NotifyCodes:
    WantedNotWanted, WantedNotFound, KillerWanted, MaxWanted, SubscriptionOk, HuntAlready, SubmitOk, WantedKilledImprisoned, NoMoney, MinPrice, OrderCompleted, SelfImprisoned, DonateSelf, WantedOffline, WantedOnline, VictimWanted = xrange(1, 17)


MIN_PRICE = 10000
COMISSION = 0.1
MAX_WANTED = 15